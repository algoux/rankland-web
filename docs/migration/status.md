# RankLand Migration Status

This file is the quick global dashboard for the RankLand migration. Update it at the end of every verified slice or when a slice's state materially changes.

## Current Focus

- Active branch: `migration/live-page-foundation`
- Current slice: PAR-006 final visual review / app logo asset parity
- Latest slice commit: `fix: 还原应用 Logo 资产`
- Last recorded full gate: passed on 2026-05-31 for PAR-006 final visual review / app logo asset parity; old/new route visual review promoted `PAR-006B` and `PAR-006C` as ready follow-ups, `PAR-006A` replaced the shared app logo with the old React `RL` asset, and app-shell overflow coverage was stabilized to wait for async shell styles. Gate used Node `v24.11.1` and pnpm `8.15.9`; `git diff --check` passed, `gen:client-router` generated 6 client routes, `test:migration` passed with build, 39 unit files / 159 unit tests, 1 SSR smoke test, 1 shallow Playwright test, and 61 passed / 1 skipped full-chain Playwright tests.
- Next recommended focus: `PAR-006B` Ant Design primary color visual parity, then `PAR-006C` Collection category logo size parity

## Route Progress

| Route | Render | Status | Branch / Slice | Coverage | Remaining parity / risk |
| --- | --- | --- | --- | --- | --- |
| `/` | SSR | Foundation + app shell + legacy app shell layout/logo class parity + legacy header right-menu wrapper parity + legacy main.normal-content/home-intro wrapper DOM parity + legacy `div.block` content group DOM parity + legacy normal-content/block spacing parity + legacy home hero title inline font-size parity + legacy home hero paragraph `text-base` class parity + legacy home card row horizontal-only gutter parity + legacy home card column `mb-4` class parity + legacy home tool logo `mr-3 inline-block` class parity + legacy home about separator `mx-2` class parity + legacy `h1.block-title` section heading parity + legacy statistics nullish fallback parity + legacy statistics `strong` typography parity + Ant Design Vue shell/content product parity + legacy global body text color/typography parity + legacy home non-card dark text color parity + legacy contact modal Ant Design parity + legacy contact QQ image class parity + legacy ContactUs trigger anchor DOM parity + legacy home external link rel omission DOM parity + legacy beian link rel omission DOM parity + legacy app shell site-switch link rel omission DOM parity + legacy app shell site-switch dropdown content class/style parity + legacy app shell site-switch branch word-break parity + legacy app shell site-switch trigger `px-2` class parity + legacy home card dark Ant Design parity + legacy header box model parity + legacy nav menu line-height/primary-color parity + legacy site-switch button parity + legacy mobile shell metrics parity + legacy recommendation card heading/icon parity + legacy card title `mr-3` spacing parity + legacy card paragraph `mt-4 mb-0` spacing parity + legacy paste.then.ac logo padding parity + analytics parity + legacy title separator parity + visual review + completion audit verified | `migration/live-page-foundation` | SSR smoke, shallow E2E, full-chain E2E, app shell/theme/bootstrap/light-background/global body light/dark text color plus old 14px/system-font/tabular-nums/1.5715 line-height typography coverage, analytics/site-switch ArrowRight icon/full-chain title-navigation E2E, app shell legacy root `layout`, header `flex justify-between`, computed `space-between`, inner `.logo` 64px box, 40px logo image, old header right-menu plain `DIV` wrapper as the third direct `.app-header-inner` child with no class/inline style containing `[data-id="app-site-switch"]`, and site-switch no longer direct-child coverage, app shell legacy desktop 50px header padding/no centered max-width/no inner gap, app shell legacy nav menu 46px line-height and dark primary `#f6ac06` selected/underline color, app shell legacy site-switch 32px height/0px min-height/2px border-radius plus old `px-2` trigger class token, 8px horizontal padding, target `_blank`, and omitted `rel` coverage, app shell legacy site-switch dropdown link branch word-break coverage: China branch `word-break: keep-all` and `cnn` global branch no inline word-break via unit/source guard, title/subtitle `mb-0`, subtitle span `opacity-60 text-xs`, 12px font size, and 0.6 opacity coverage, app shell mobile legacy 20px header padding/no inner padding/no inner gap, 64px header/logo container, 40px logo image, 16px nav item padding, and 8px site-switch padding coverage, Ant Design Vue shell DOM coverage, legacy home `main.normal-content` and `.home-intro` wrapper DOM coverage, legacy home `div.block` content group DOM coverage, legacy home normal-content desktop 32px/50px padding, mobile 32px/20px padding, no max-width cap, block 40px top spacing, block title 20px bottom spacing, home hero title old inline `font-size: 32px` plus computed 32px coverage, home hero paragraph `text-base` class token coverage, home recommendation/tool card rows old horizontal-only `gutter={16}` coverage with `rowGap: normal`, home recommendation/tool card columns old `mb-4` class token plus 16px bottom margin coverage, home tool logo old `mr-3 inline-block` class token plus inline-block display and 12px right margin coverage, home about separator old `mx-2` class token plus 8px left/right margin coverage, `h1.block-title` DOM, 32px/500 section heading typography coverage, partial upstream statistics `-` fallback coverage across SSR/hydration, and total SRK count `strong` DOM plus bold/non-italic presentation coverage, Ant Design Vue home Card/Row/Col coverage including old dark card background `#141414`, border `#303030`, radius 2px, and inherited text color coverage, old home non-card hero/resource/about text dark color coverage, old recommendation card `h2` plus `UnorderedListOutlined`/`TrophyOutlined` icon coverage, old card title icon/logo `mr-3` right-spacing coverage, old card paragraph `mt-4 mb-0` DOM plus 16px/0px margin coverage, old paste.then.ac logo 24px/2px padding coverage, old visible Home external links keep target `_blank` and omit `rel` coverage, conditional old Home beian link keeps target `_blank`, configured text, and omitted `rel` under `cnn` coverage, old contact modal Ant Design dark content/title/close/body padding coverage, old contact QQ image `w-full` class coverage, old Home ContactUs no-`href` anchor trigger DOM coverage, app shell desktop/mobile screenshot review, home desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/search` | CSR | Foundation + legacy Head canonical omission parity + legacy normal-content shell DOM parity + legacy state wrapper DOM parity + legacy section content DOM parity + legacy row content DOM parity + legacy row container DOM parity + legacy error message DOM parity + legacy heading DOM parity + legacy input DOM parity + legacy result count plain-text DOM parity + legacy keyword whitespace/query serialization behavior parity + Ant Design Input.Search/List/Spin/default search icon button parity + legacy state/list/empty-state spacing/dark text parity + legacy load-error DOM/color/class parity + hydration marker visual parity + legacy row metadata utility-class/typography parity + legacy h3 heading parity + zero-result display parity + view-count Eye icon parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, generated route, full-chain E2E including old route-local Head omits canonical link, legacy root `div.normal-content`, plain inner `div`, no Vue-only `.search-page`, no `section.search-panel`, no route-local `min-height: 70vh`, legacy loading/error/result/recent state wrappers using only old `mt-10` contract with no Vue-only `.search-state` / `.search-section`, result/recent wrappers restored to `DIV`, result/recent title nodes restored to old `div.opacity-70` with no Vue-only `.search-section-title`, result count restored to exact old plain text `搜索到 N 个结果` with no Vue-only `[data-id="search-result-count"]` span while preserving section `data-result-count`, keyword whitespace restored to old React passthrough semantics so route `kw` and submitted search values are not trimmed before the input or Fuse search, shared route builders now preserve old `urlcat` query serialization with `+` spaces and empty `kw=`, whitespace-only `kw` remains a result state, empty submitted search navigates to `/search?kw=` while rendering recent-list state, list spacing restored to old `div.mt-2 > .ant-list` with no Vue-only `.search-list`, recent empty state restored to old `div.mt-2` with no Vue-only `.search-empty-state`, row container restored to old Ant Design list item default with no Vue-only `.search-list-item` and `display: flex`, row content restored to exact old `p.mb-0`, `span.ml-2.opacity-70`, and `p.mb-0.opacity-50.text-sm` classes with no Vue-only `.search-row-title`, `.search-view-count`, or `.search-created-at`, load-error message restored to exact old `div.text-red-500` class with no Vue-only `.search-error-message`, heading restored to exact old `h3.mb-6` class with no Vue-only `.search-heading`, input restored to old no-page-custom-class contract with no Vue-only `.search-input` and 0px wrapper top margin, legacy normal-content wrapper/padding and inherited text color coverage, legacy h3 heading coverage, hidden hydration marker coverage, Ant Design search control/list/loading coverage, old loading `mt-10` Spin class token coverage, old boolean `enterButton` default SearchOutlined icon button coverage, legacy load-error two-level DOM with outer `mt-10` wrapper and inner `text-red-500` message, message/40px spacing/color coverage, legacy `mt-10` result/recent section, `opacity-70` title/view-count, `mt-2` list and recent empty-state, `mb-0` row title/created-at, `ml-2` view-count, and `opacity-50 text-sm` created-at utility class token coverage, zero-result no-extra-empty-state coverage, recent-empty dark text color coverage, and recent/result view-count Eye icon coverage, search desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/ranklist/:id` | SSR | Foundation + hidden hydration marker parity + Not Found Ant Design action parity + generic load error action parity + loading spinner parity + legacy state wrapper DOM parity + route content utility-class/spacing parity + loaded wrapper DOM parity + shared wrapper header utility-class/header banner mb-2 spacing/header banner image class/inline-style parity/header action utility-class/header action gap/header action display/header meta child DOM/header title typography/header metadata text-size/contributor item span DOM/contributor link rel DOM/ref-links span DOM/ref-link item span DOM/ref-link rel DOM/view-count icon/view-count utility-class/view-count missing fallback/icon hover dropdown/action icon primary-color/action separator/export-group/caret/share notification/header-to-progress/progress wrapper utility-class/link primary-color/extra ref-link trigger color/extra ref-link spacing/header meta text color parity + Ant Design Vue filter controls/organization selected-tag/unique options/internal spacing parity + controls-row utility-class/spacing parity + table-wrapper offset/top-spacing/table spacer DOM parity + SRK remarks wrapper exact-class/pill placement/style/legacy primary border color parity + SRK table theme/table-header box model/status-block score-details spacing parity + SRK asset image error parity + old SRK dependency baseline parity + user modal title/body/dark text color/responsive-width/organization line class/organization line spacing/team members class/team members spacing/team separator raw text/team member entry DOM/unofficial line class/unofficial line spacing/segment line class/segment line spacing/segment label class/segment label style/slogan spacing class/slogan style/font asset/marker label/photo wrapper class/photo-slogan shared wrapper/empty photo wrapper/photo width/rank-time chart-only wrapper class chrome parity + runtime `_now` export cleanup parity + footer utility-class/footer paragraph/footer external link rel omission DOM/footer contact trigger anchor DOM/footer beian rel omission DOM/footer beian state parity + G2 rank-time tooltip/animation parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E including hidden hydration marker coverage, loaded root `DIV`, loaded content `DIV`, exact loaded content class list `mt-8 mb-8` with no Vue-only `ranklist-content` class, loaded content old `mt-8 mb-8` class token plus 32px spacing coverage, NotFound Ant Design primary/small return button, generic ranklist load error `mt-16 text-center` and Ant Design primary small Refresh coverage, old NotFound/error/loading state wrappers restored to exact plain `DIV.mt-16.text-center` DOM with no Vue-only `ranklist-state` class, loading Spin renders as a child instead of the state root, old SRK header title 32px/500/4px typography coverage, old header/banner utility classes including banner wrapper `flex items-center justify-center` with 0px bottom margin, banner image exact `mb-2` class, inline `max-width: min(100%, 1820px)` / `max-height: 40vh`, no Vue-only `rankland-ranklist-banner` product class, and 8px bottom margin, title `text-center mb-1`, meta `text-center mt-1`, contributors `mb-0`, ref-links, and time `text-center mb-0`, old header contributors/ref-links child DOM under the meta block, old broken SRK banner and user photo image hiding coverage, old SRK header view-count/contributors/ref-links/time 14px text-size coverage, old contributor visible item-level `span` DOM coverage, old contributor link target `_blank` plus `rel=\"noopener\"` coverage, old reference-link wrapper `span` DOM and visible item-level `span` DOM coverage, old visible and hidden reference-link target `_blank` plus `rel=\"noopener\"` coverage, old Eye icon view count with `mr-2` utility class including missing-`viewCnt` `-` fallback coverage, contributors, reference links, hidden reference-link caret trigger coverage, old hidden reference-link trigger inherited header text color and zero CSS left-margin coverage, old header meta/action no extra flex gap coverage, old header meta/action block/inline display coverage, old header meta/view-count/ref-link-label/time light `rgba(0, 0, 0, 0.85)` and dark `rgba(255, 255, 255, 0.85)` text color coverage, old header ref-link and footer contact trigger light `#ff8104` / dark `#f6ac06` color plus light `#ff9d2e` / dark `#a7770b` hover coverage, old header export/share action utility classes including export `border-0 border-solid border-gray-400 mr-2` plus conditional `pl-2 border-l` and share `pl-2 border-0 border-l border-solid border-gray-400`, old header export/share action icon light `#ff8104` / dark `#f6ac06` color plus light `#ff9d2e` / dark `#a7770b` hover coverage, old header-to-progress spacing coverage, old progress wrapper `mx-4` utility class plus 16px horizontal margin coverage, Ant Design Vue Download/Share icon trigger coverage, old `pl-2 border-l` header action separator coverage, old `导出为` export menu group coverage, runtime `_now` cleanup for SRK/converter-backed exports, Ant Design copy-link/copy-embed success notification coverage, hover export/share dropdown open-close, Ant Design Vue Select/Switch/Radio filter controls and official-only semantics, old controls root `mt-3 mx-4 flex justify-between items-center`, Select `ml-2`, official-filter `ml-5 inline-flex items-center`, official label `mr-1`, marker-filter `ml-5 inline-flex items-center` utility class coverage, old filter-control `ml-2`/`ml-5`/`mr-1` spacing coverage, old progress-to-controls `mt-3` spacing coverage, old organization-filter `已选择 N 个` compact selected tag and unique options coverage, hydration-safe filter reload coverage, standalone table-wrapper `ml-4` offset coverage, old SRK table-wrapper `mt-6` top-spacing and table spacer DOM coverage, old SRK remarks wrapper exact `mb-4 text-center` class list with no Vue-only `rankland-ranklist-remarks`, 16px bottom margin and centered alignment, wrapper-local `.srk-remarks` pill style plus light `rgba(255, 129, 4, 0.8)` and dark `rgba(246, 172, 6, 0.8)` border coverage, low-level SRK dark theme problem-header coverage, macOS Blink optimized low-level SRK table-header `box-sizing: content-box` coverage, SRK status-block score-details `margin-top: 0` coverage, old SRK dependency baseline unit coverage, user modal title-only username/no duplicate body heading coverage, user modal dark body text color `rgba(255, 255, 255, 0.85)` coverage, user modal legacy responsive width coverage, user modal organization-line old `mb-0` class plus zero-margin coverage, user modal team-members row old `mt-2` class plus spacing, raw separator text ` / `, and item-level entry span DOM coverage, non-official notice line old `mt-4 mb-0` class plus spacing coverage, `所在奖区` segment line old `mt-4 mb-0` class plus spacing coverage, `所在奖区` segment label old `.user-modal-segment-label` class plus `bg-segment-*` and style coverage, `x_slogan` old `.slogan mt-4 mb-2` classes plus style, 16px/8px spacing, and ZCOOL XiaoWei font-face load coverage, SRK preset marker label, old photo/slogan shared `mt-4` wrapper structure plus 16px top margin and full-width photo coverage, old empty photo/slogan wrapper for no-photo users, old chart-only rank-time `mt-4` wrapper class with no unit/summary/event chrome coverage, old footer root `text-center mt-8` and paragraph `mb-0` / `mt-1 mb-0` utility class coverage, old footer paragraph `mt-1 mb-0` spacing coverage, old footer GitHub/Standard Ranklist/榜单合集 external links keep target `_blank` and omit `rel` coverage, old footer ContactUs no-`href` anchor trigger DOM coverage, conditional China-site beian footer state plus beian link target `_blank`, configured text, and omitted `rel` coverage, G2 rank-time chart metadata/canvas coverage, footer contact modal, ranklist desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/collection/:id` | SSR | Foundation + hidden hydration marker parity + loaded wrapper DOM parity + Not Found Ant Design action parity + generic load error action parity + loading spinner parity + legacy state wrapper DOM parity + Ant Design Vue menu/category icon/mobile collapse/collapse-icon product parity + legacy menu leaf anchor DOM parity + legacy menu open keys interaction parity coverage + legacy collapse button wrapper DOM parity + legacy collapsed submenu padding parity + legacy ranklist panel layout parity + legacy shell class/nav z-index parity + legacy nav chrome color/hidden-header DOM/title style parity + exact remaining-height/pixel animation parity + empty-selection heading parity + selected-ranklist loading/action/error parity + selected-ranklist shared wrapper/content utility-class/spacing/view-count icon parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E, hidden hydration marker coverage, loaded root `DIV`, loaded shell `DIV.srk-collection-container`, nav `DIV.srk-collection-nav`, old collapse button plain wrapper `DIV` coverage with no class/inline style and collapse button as the wrapper's direct child, hidden header `DIV.srk-collection-hidden-header`, ranklist panel `DIV.srk-collection-ranklist`, selected content `DIV.pb-8`, no Vue-only `collection-page` / `collection-nav` / `collection-hidden-header` / `collection-ranklist-panel` / `collection-ranklist-content` product classes, and selected content 32px bottom padding coverage, NotFound Ant Design primary/small return button, generic collection load error `pt-16 text-center` and Ant Design primary small Refresh coverage, old collection-level NotFound/error/loading plus selected-ranklist error/loading state wrappers restored to exact plain `DIV.pt-16.text-center` DOM with no Vue-only `collection-state` class, loading Spin renders as a child instead of the state root, Ant Design Vue `MenuFoldOutlined`/`MenuUnfoldOutlined` collapse icon component coverage, layout helper coverage, Ant Design Vue menu/category icon/mobile collapse coverage, old ranklist leaf menu label anchor `A` coverage with `/collection/official?rankId=test-key` href and no Vue-only `role` / `aria-current`, old directory menu label `SPAN` coverage, selected directory submenu auto-opens for `rankId=test-key` and can be manually closed without route changes, old collapsed submenu-title `padding-left: 0px` and `padding-right: 0px` coverage, old `.srk-collection-ranklist` `position: relative` and `flex: 1` coverage, old `.srk-collection-container` / `.srk-collection-nav` / `.srk-collection-ranklist` shell class, old nav `z-index: 1`, and hidden-header `z-index: auto` coverage, old nav light `#f4f4f4` / dark `#111111` background and dark `#434343` border coverage, old `.srk-collection-hidden-header h3.mb-0` DOM coverage, old hidden-title no-inline-`marginTop` style coverage, old expanded `marginLeft: 8px`, collapsed `fontSize: 14px` / `marginLeft: 0px`, and no Vue-only `line-height: 1` coverage, legacy remaining-height/nav animation contract coverage, empty-selection `h3.pt-16.text-center` coverage, selected-ranklist error `pt-16 text-center` and Ant Design primary small Refresh coverage, selected-ranklist wrapper header/filter/progress/footer/action coverage, selected-ranklist Eye icon view count, selected-ranklist old `pb-8` class token plus 32px bottom padding coverage, collection desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/playground` | CSR | Foundation + legacy Head canonical omission parity + old desktop flex/500px editor/preview flex layout + legacy shell class parity + no Vue-only editor wrapper border/radius chrome + no visible toolbar/heading chrome + no visible preview action chrome + hidden hydration marker parity + hidden editor-ready marker parity + Ant Design Vue welcome modal old docs cue/icon copy + Monaco editor/schema/theme/remaining-height product parity + legacy Monaco minimap/default options parity + legacy live valid-source preview sync parity + invalid JSON prompt h3 utility-class/spacing/tag `mr-0` class-token parity + preview docs-link plain anchor/wrapper exact class/DOM/icon placement/rel omission DOM parity + valid preview wrapper `div.mt-8.mb-8` DOM/class parity + preview SRK header/filter/progress/wrapper/check-error DOM/render-error Alert parity + SRK check-error wrapper class parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E, old route-local Head omits canonical link, old desktop `display:flex` layout, old `.srk-playground-container` root class, old 500px editor pane, old no editor wrapper border/radius coverage, old `.srk-playground-preview` preview pane class, old `flex:1` preview pane, old no page-level `Playground` heading/toolbar coverage, old no visible Preview button coverage, layout-hidden hydration marker coverage, hidden Monaco editor-ready marker coverage, one-time welcome modal/localStorage coverage including old right-top `QuestionCircleOutlined` docs cue copy, Monaco readiness/schema/theme coverage, old Monaco minimap/default option chrome coverage with `.minimap` visible and nonzero-width while preserving only old `selectOnLineNumbers: true`, unit/source-guard coverage that Monaco `@change`, `Ctrl/Cmd + S`, and the stable E2E hook share the same draft-source + parse-state sync transition, old preview-pane `div.absolute.right-4.top-4` wrapper with exact class `absolute right-4 top-4`, no Vue-only wrapper class, plain no-class `QuestionCircleOutlined` srk docs anchor, target `_blank`, and omitted `rel` coverage, invalid JSON prompt without parser-detail plus old `h3.mt-16.text-center` class/spacing and shortcut tag `mr-0` class token plus 0px right/left spacing coverage, old valid SRK preview wrapper `div.mt-8.mb-8` plus 32px top/bottom margin coverage, shared Ant Design Vue preview header/title/time/filter/progress coverage, old checker-error exact `div.ml-8 > h3 + pre` DOM/class coverage for structurally invalid object JSON with no Vue-only `.rankland-ranklist-check-error` product class, old Ant Design render-error Alert role/icon/message/description/400px centered spacing coverage, no extra row-count metadata coverage, deterministic no-animation playground desktop/mobile viewport screenshot review, `test:migration` | Exact old Monaco `0.34.0` package-version parity and synthetic Monaco editing in Playwright remain deferred risk; product editor path now restores old React live valid-source preview sync through Monaco `@change`, while schema/theme, default minimap chrome, and stable preview coverage are verified by unit/source guards plus hydration/build/full-chain tests. Mobile keeps the migrated no-horizontal-overflow guard instead of reproducing old fixed-width overflow behavior. |
| `/live/:id` | CSR | Foundation + legacy Head canonical omission parity + hidden hydration marker parity + parity follow-ups + Not Found Ant Design action parity + generic load error action parity + loading spinner parity + legacy state wrapper DOM parity + route content utility-class/spacing parity + legacy loaded wrapper chrome parity + legacy root wrapper DOM/min-height parity + legacy content wrapper DOM parity + reconnect/backoff + Toastify DOM/animation/pixel/order parity + shared Ant Design Vue filter/share notification controls + Ant Design Vue scroll-solution Switch parity + legacy scroll toggle utility-class/mr-1 spacing/text style/reload parity + legacy mobile no-toggle DOM parity + legacy controls extra-action no-gap chrome parity + legacy unknown result class fallback parity + conditional no-meta header action separator/action utility-class parity + table-wrapper offset + runtime `_now` export cleanup parity + user modal rank-time chart-only wrapper class chrome + G2 rank-time tooltip/animation parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E including old route-local Head omits canonical link while preserving title and loaded-state `og:title`, hidden hydration marker coverage, loaded content old `DIV` wrapper with exact `mt-8 mb-8` class list and no Vue-only `live-content` / `live-content-with-scroll-solution` product classes, 32px spacing, old inline `250px` scroll-solution offset, and mobile realtime content-left `250px` coverage, NotFound Ant Design primary/small return button, generic live load error fixed-message/no-debug-detail and Ant Design primary small Refresh coverage, legacy Ant Design Spin loading coverage while live info is loading, old NotFound/error/loading state wrappers restored to exact plain `DIV.mt-16.text-center` DOM with no Vue-only `live-state` class, loading Spin renders as a child instead of the state root, loaded root `DIV` with no Vue-only `live-page` class and no route-local `70vh` min-height, loaded live wrapper no route padding/no width cap/no auto-centering plus old `250px` scroll-solution left offset coverage, WebSocket error reconnect, unexpected WebSocket close reconnect, Ant Design Vue scroll-solution Switch coverage, old scroll-solution toggle wrapper `inline-flex items-center` class tokens, old text span `mr-1`, old 4px text-to-switch spacing from text margin instead of wrapper gap, old 250ms `URLSearchParams(window.location.search)` plus `window.location.search` browser reload behavior with query preservation and `beforeunload` coverage, wrapper `column-gap` / `row-gap: normal`, old 14px and light `rgba(0, 0, 0, 0.85)` scroll-solution toggle text style coverage, old mobile `clientWidth < 768` no-toggle-DOM behavior for `[data-id="live-scroll-solution-toggle"]` and `.live-scroll-toggle`, old SRK controls root `mt-3 mx-4 flex justify-between items-center` with no declared gap plus plain block extra-action wrapper coverage, scroll-solution unknown results keep old `result`-only fallback and `--` text with no Vue-only `result-unknown` class, scroll-solution toggle reload disable path, hidden internal status marker, desktop/mobile realtime layout bounds, old Toastify `DIV` container plus direct visible `DIV` toast row DOM with no `ul` / `li` product structure, legacy Toastify container/Zoom presentation, old Toastify `newestOnTop={false}` oldest-first visible row order coverage, shared Ant Design Vue filter interaction, no-meta export/share header action separator coverage, no-meta export action keeps old `border-0 border-solid border-gray-400 mr-2` without `pl-2 border-l` while share keeps `pl-2 border-0 border-l border-solid border-gray-400`, runtime `_now` cleanup for SRK/converter-backed exports, shared copy-embed success notification coverage, standalone table-wrapper `ml-4` offset coverage, old chart-only user modal rank-time `mt-4` wrapper class with no unit/summary/event chrome, G2 rank-time chart metadata/canvas coverage, normal live desktop/mobile screenshot review, mobile progress label bounds, and `test:migration` | Product polish after route parity review |
| `/:catchAll(.*)` | CSR fallback | Legacy fallback 404 copy/status/spacing parity verified + scaffold route exposure removed | `migration/live-page-foundation` | Full-chain E2E covers unknown public route, scaffold `/about`, and scaffold `/demo/detail/123` as HTTP 404 with legacy `404 Not Found · 你来到了榜单荒地`, `mt-32`, `text-xl`, and centered layout; route generator/unit coverage verifies only six migrated RankLand public routes are exposed | Product polish after route parity review |

## Infrastructure Progress

| Area | Status | Notes |
| --- | --- | --- |
| Node / package manager | Done | Node 24, pnpm 8 |
| RankLand API service | Done | Normal/CDN API adapters, cache behavior, error mapping |
| Route builders | Done | Public route builders and generated route coverage. `corepack pnpm run gen:client-router` now exits after one-shot generation; `gen:client-router:watch` preserves dev-time watch mode. |
| SRK Vue wrapper | Foundation + live controls + live scroll toggle utility-class parity + modal clicks + G2 rank-time user modal + old chart-only rank-time modal chrome + user modal responsive width + user modal title/body/dark text color/organization line class/organization line spacing + user modal team members class/team members spacing/team separator raw text/team member entry DOM/unofficial line class/unofficial line spacing/segment line class/segment line spacing/segment label class/segment label style/slogan spacing class/slogan style/font asset/marker label/photo wrapper class/photo-slogan shared wrapper/empty photo wrapper/photo width + asset URL rewriting + old broken asset image hiding + basic export/share + converter-backed exports with runtime `_now` cleanup + old export menu group + share-copy Ant Design notifications + render-error wrapper DOM/class parity + render-error Ant Design Alert + footer contact prompt/link color + old footer contact trigger anchor DOM + old footer root and paragraph utility classes + old footer paragraph spacing + old footer external package/project links omitted `rel` DOM + conditional China-site beian footer + old header utility classes + old header action utility classes + old header action no-gap spacing + old header action block/inline display + old header meta child DOM parity + old header title 32px/500/4px typography + old header metadata/contributors/ref-links/time 14px text size + old contributor item `span` DOM + old contributor link `rel=\"noopener\"` DOM + old ref-links `span` wrapper DOM + old ref-link item `span` DOM + old ref-link `rel=\"noopener\"` DOM + header metadata + old header metadata light/dark text colors + old header-to-progress spacing + old progress wrapper `mx-4` spacing + old header time format + Eye icon view count, exact old `mr-2` class list with no Vue-only `.rankland-ranklist-view-count`, and missing-`viewCnt` fallback + old header ref-link primary colors + old header export/share action icon primary and hover colors + old hidden ref-link trigger inherited text color and zero CSS margin-left + Ant Design Vue icon hover dropdowns/caret triggers + old conditional header action separators + Ant Design Vue filter controls/internal spacing + old controls/filter utility classes + old controls root no declared gap and plain extra-action wrapper chrome + old organization-filter compact selected tag/unique options + hydration-safe filter reload coverage + old progress-to-controls `mt-3` spacing + collection selected-wrapper controls + playground preview header + standalone table-wrapper offset + old `mt-6` table top-spacing + old table spacer DOM + old modal wrapper table-container DOM + old remarks wrapper `mb-4 text-center` utility classes + wrapper-local remarks pill with old primary rgba border colors + low-level table theme propagation + optimized table-header box model + status-block score-details spacing + null status fallback problem-alias text + old `@algoux/standard-ranklist@0.3.12` / `@algoux/standard-ranklist-utils@0.2.13` dependency baseline + default preview progress done | Remaining lower-level exact table pixel parity is product/visual review driven |
| Full-chain E2E harness | Done | Real bwcx/Koa app plus controlled mock RankLand backend |
| Vue app shell | Foundation + Ant Design Vue product parity + legacy layout/logo class parity + legacy header nav wrapper parity + legacy global body text color/typography parity + legacy document body bootstrap parity + legacy contact modal Ant Design parity + legacy contact QQ image class parity + legacy ContactUs trigger anchor DOM parity + legacy header box model parity + legacy nav menu line-height/primary-color parity + legacy site-switch button/current URL/filtering parity + legacy site-switch trigger `px-2` class parity + legacy site-switch link rel omission DOM parity + legacy site-switch dropdown content class/style/product-class parity + legacy site-switch branch word-break parity + legacy mobile metrics parity + theme sync + analytics + legacy title separator + fallback 404 + visual review verified | Logo, primary navigation, site switch dropdown, old root `layout` class, old header inner `flex justify-between` classes with computed `space-between`, old header middle navigation wrapper `DIV` direct child with inline/computed `flex: 1 1 0%` and `min-width: 0px` around `[data-id="app-nav"]`, old inner `.logo` box at 64px by 64px, 40px logo image, old `ArrowRightOutlined` external-link cue via Ant Design Vue, old site-switch trigger `px-2` class token with 8px computed horizontal padding, old site-switch link `target="_blank"` with omitted `rel`, old site-switch current URL filtering that omits `focus` / `聚焦` controls and hash fragments while preserving ordinary query segments, old site-switch dropdown link branch style contract: default China branch computes `word-break: keep-all` while the `cnn` global branch computes no inline word-break, old title/subtitle direct paragraph exact `p.mb-0` class lists with zero bottom margin and no Vue-only `app-site-switch-title` / `app-site-switch-subtitle` product classes, old subtitle span `opacity-60 text-xs` class tokens with 12px font size and 0.6 opacity, old desktop 50px header padding with no centered max-width/no inner gap, old nav menu 46px line-height, old dark nav primary `#f6ac06` selected/active/hover color and underline, old site-switch 32px button height/0px min-height/2px radius with 8px horizontal padding, old mobile 20px header padding/no inner padding/no inner gap, old mobile 64px header/logo container, 40px logo image, 16px nav item padding, 8px site-switch padding, old `Title \| RankLand` document-title separator on direct load and CSR navigation, old light-mode `#f2f2f2` body background, old global body text color light `rgba(0, 0, 0, 0.85)` and dark `rgba(255, 255, 255, 0.85)`, old global body typography 14px/system-font/tabular-nums/1.5715 line-height, old shared contact modal via Ant Design Vue Modal with dark content/title/close/body padding coverage, old ContactUs no-`href` anchor trigger DOM coverage, plus old contact QQ image `w-full` class coverage, BackTop, legacy focus-mode shell bypass, old document body inline bootstrap style, Dark Reader cleanup script before app mount, hydrated body opacity release to `1`, pre-hydration theme bootstrap, system theme class sync, macOS Blink optimization class, GA initialization/pageview dispatch, legacy fallback 404 copy/status/spacing, desktop/mobile viewport bounds, and screenshot review are covered by full-chain E2E; `tests/unit/app-site-switch.spec.ts` guards the `cnn` branch in the default unit gate. Ant Design Vue `a-menu` is mounted client-only to avoid its ResizeObserver SSR hydration mismatch. |
| Migration process | Final review evidence refreshed | Slice workflow, conversation I/O protocol, this dashboard, completion audit, manual acceptance checklist, and final integration review are in `docs/migration` and `docs/superpowers`; 2026-05-28 docs now reflect the latest 38 unit files / 158 unit tests / 61 passed + 1 skipped full-chain product-parity gate instead of the older route-compatible-only review |

## Deferred Product Decisions

- Home: old React `main.normal-content` and `.home-intro` wrapper DOM, old `div.block` content group DOM, `normal-content` desktop/mobile padding, no desktop max-width cap, `home-intro` block spacing, hero title inline `font-size: 32px`, hero paragraph `text-base` class token, recommendation/tool card row horizontal-only gutter with no extra row gap, recommendation/tool card column `mb-4` class tokens with 16px bottom spacing, tool-card logo `mr-3 inline-block` class tokens with inline-block display, about separator `mx-2` class token with 8px left/right spacing, `h1.block-title` section headings, statistics `?? '-'` nullish fallback, total SRK count `strong` bold/non-italic presentation, Ant Design recommendation/tool card layout is restored with Ant Design Vue Card/Row/Col, including old dark Ant Design Card background/border/radius/text color, non-card hero/resource/about dark text color, recommendation card `h2` headings, `UnorderedListOutlined`/`TrophyOutlined` icons, old `mr-3` icon/logo right spacing, old card paragraph `mt-4 mb-0` 16px/0px spacing, and the paste.then.ac logo's old `24px` size plus `2px` padding, while preserving SSR statistics, canonical/OG/JSON-LD, shared Ant Design Vue contact modal, old ContactUs no-`href` anchor trigger DOM, old contact QQ image `w-full` class, and app-shell viewport bounds.
- Fallback 404 / public route surface: old React `404 Not Found · 你来到了榜单荒地` copy, exact `text-center mt-32 text-xl` class tokens, 128px/20px/center layout, and HTTP 404 status for unknown public routes are restored; generated client/server route maps now expose only `/`, `/search`, `/ranklist/:id`, `/collection/:id`, `/playground`, and `/live/:id`, with scaffold `/about` and `/demo/detail/:id` excluded and covered as fallback 404s.
- Page titles: old React `formatTitle` behavior using `Title | RankLand` is restored across direct route loads and CSR navigation; empty titles still render `RankLand`.
- Ranklist: old loaded-state Head now renders `og:title`, absolute `og:url`, and absolute canonical URL using the old React production site origin; old React `Spin` loading fallback with `mt-16 text-center` layout is restored for the no-ranklist state; NotFound and generic error wrappers now also carry the old `mt-16 text-center` class tokens, with the NotFound heading carrying old `h3.mb-4`; route-level asyncData still blocks initial client navigation until ranklist data is available; the migrated hydration marker remains available for tests but is visually hidden, absolute-positioned, and `aria-hidden` so it does not participate in product layout or the accessibility tree.
- Search: old route-local Head omits canonical link while preserving title and `og:title`, old React root `div.normal-content`, plain inner `div`, no Vue-only `.search-page`, no `section.search-panel`, no route-local `min-height: 70vh`, loading/error/result/recent state wrappers using only old `mt-10` contract with no Vue-only `.search-state` / `.search-section`, result/recent wrappers restored to `DIV`, result/recent title nodes restored to old `div.opacity-70` with no `.search-section-title`, list spacing restored to old `div.mt-2 > .ant-list` with no `.search-list`, recent empty state restored to old `div.mt-2` with no `.search-empty-state`, row containers restored to old Ant Design list item default with no `.search-list-item`, row content nodes restored to exact old `p.mb-0`, `span.ml-2.opacity-70`, and `p.mb-0.opacity-50.text-sm` classes with no `.search-row-title`, `.search-view-count`, or `.search-created-at`, load-error inner message restored to exact old `div.text-red-500` class with no `.search-error-message`, heading restored to exact old `h3.mb-6` class with no `.search-heading`, search input restored to old no-custom-class DOM with no `.search-input`, `normal-content` wrapper/padding and inherited old body text color, `h3.mb-6` heading, no visible hydration/test marker, Ant Design `Input.Search` with default boolean `enterButton` SearchOutlined icon button, shared route-builder query serialization now matches old `urlcat` for `+` spaces and empty `kw=`, `List`, `Spin`, `EyeOutlined` view-count icon, loading `mt-10` Spin class, load-error outer `mt-10` wrapper plus inner `text-red-500` message DOM/color, result/recent `mt-10` section, `opacity-70` section title/view-count, `mt-2` list/recent-empty spacing, `mb-0` row paragraphs, `ml-2` view-count, `opacity-50 text-sm` created-at, recent-empty dark text color, row metadata opacity/spacing typography, and zero-result summary-only behavior are restored for the search form, loading/error state, recent rows, and result rows.
- Collection: loaded-state Head now matches old React with `og:title`, absolute `og:url`, and absolute canonical URL; Ant Design Vue inline menu, category logo assets, old React `MenuFoldOutlined`/`MenuUnfoldOutlined` collapse icons via Ant Design Vue components, mobile collapse behavior, old ranklist leaf menu labels render as anchors with collection `rankId` href and no Vue-only `role` / `aria-current` attributes while directory labels remain spans, selected-path submenu open-key behavior is covered so `dir-icpc` auto-opens for `rankId=test-key` and can be manually closed without route changes, old collapse button plain wrapper `DIV` with no class/inline style and the button as its direct child, old collapsed submenu title 0px horizontal padding, old loaded shell root no longer carries Vue-only root `position` / `min-height` chrome and computes `position: static` / `min-height: 0px`, old `.srk-collection-ranklist` `position: relative` and `flex: 1`, old selected-ranklist `pb-8` class token and 32px bottom padding, collection-level NotFound/error/loading plus selected-ranklist error/loading now restore exact old plain `DIV.pt-16.text-center` DOM with no Vue-only `collection-state` class, loading Spin renders as a child instead of the state root, NotFound heading carries old `h3.mb-4`, empty-selection `h3.pt-16.text-center`, generic collection load error and selected-ranklist error use Ant Design primary small Refresh actions, old `.srk-collection-container` / `.srk-collection-nav` / `.srk-collection-ranklist` shell classes, old nav `z-index: 1`, hidden-header `z-index: auto`, old light/dark nav chrome colors, old `.srk-collection-hidden-header h3.mb-0` DOM, old hidden-title no-inline-`marginTop`, expanded `marginLeft: 8px`, collapsed `fontSize: 14px` / `marginLeft: 0px`, and no Vue-only `line-height: 1`, hidden absolute/`aria-hidden` hydration marker, exact remaining-height calculation, and nav/menu/panel pixel animation contract are restored.
- Playground: old route-local Head omits canonical link while preserving title and `og:title`, old `.srk-playground-container` shell class, old desktop flex layout with 500px editor pane, old `.srk-playground-preview` pane class with `flex: 1` preview pane behavior, editor wrapper without Vue-only border/radius chrome, old no visible page-level `Playground` toolbar/heading chrome, no visible Preview button chrome, layout-hidden absolute/`aria-hidden` hydration marker behavior, hidden absolute/`aria-hidden` editor-ready marker behavior, one-time welcome modal including old right-top `QuestionCircleOutlined` docs cue copy, Ant Design Vue shortcut tag/modal including old invalid prompt `h3.mt-16.text-center` and `Tag.mr-0` class token plus 0px spacing, Monaco editor, SRK schema diagnostics, theme-aware editor theme, remaining-height behavior, old Monaco default minimap chrome with only old `selectOnLineNumbers: true` option preserved, old preview-pane `div.absolute.right-4.top-4` wrapper with exact old class `absolute right-4 top-4` around the plain no-class `QuestionCircleOutlined` srk docs anchor plus `target="_blank"` and omitted `rel`, old invalid JSON prompt without parser-detail behavior and `mt-16 text-center` spacing, old valid SRK preview `div.mt-8.mb-8` wrapper, old SRK checker-error exact `div.ml-8 > h3 + pre` DOM/class for structurally invalid object JSON with no Vue-only `.rankland-ranklist-check-error` class, old preview header/title/time, old Ant Design render-error Alert role/icon/message/description and 400px centered spacing, no extra row-count metadata, old preview `showFilter` behavior, and default SRK progress bar behavior are restored. Exact old Monaco `0.34.0` package-version parity is intentionally not preserved because the verified Vue wrapper requires Monaco `>=0.43.0`.
- Live: old route-local Head omits canonical link while preserving title and loaded-state `og:title`, hidden absolute/`aria-hidden` hydration marker behavior, loaded content old `DIV` wrapper with exact `mt-8 mb-8` class list, no Vue-only `live-content` / `live-content-with-scroll-solution` product classes, 32px spacing, old inline `250px` scroll-solution offset, mobile realtime content-left `250px`, Ant Design Vue scroll-solution Switch with old 4px text-to-switch spacing plus old 14px / light `rgba(0, 0, 0, 0.85)` toggle text style, old 250ms `URLSearchParams(window.location.search)` plus `window.location.search` browser reload behavior for scroll-solution toggle changes, old mobile no-toggle-DOM behavior below 768px, NotFound/error/loading wrappers carry old `mt-16 text-center` class tokens, NotFound heading carries old `h3.mb-4`, generic load error fixed-message/no-debug-detail with Ant Design primary small Refresh action, legacy Ant Design Spin loading state, legacy `mt-16 text-center` state spacing, automatic WebSocket reconnect/backoff, and old Toastify `DIV` container/direct visible `DIV` row DOM and legacy Toastify container/Zoom animation behavior are restored as product enhancements.
- SRK renderer wrapper: header banner wrapper old `flex items-center justify-center` without Vue-only `.rankland-ranklist-banner-wrap`, banner wrapper `display:flex` / `align-items:center` / `justify-content:center`, banner image exact old `mb-2` class with inline `max-width: min(100%, 1820px)` / `max-height: 40vh` and no Vue-only `rankland-ranklist-banner` product class, title `text-center mb-1`, meta row exact old `text-center mt-1` without Vue-only `.rankland-ranklist-header-meta`, contributors `mb-0`, ref-links, and time `text-center mb-0`, contributors/ref-links old meta-block child DOM, old broken SRK banner/user-photo image hiding behavior, header title old 32px/500/4px typography, header metadata/contributors/ref-links/time old 14px text size, header contributors old visible item-level `span` DOM, URL contributor anchors old `target=\"_blank\"` plus `rel=\"noopener\"` DOM, header ref-links old `span` wrapper DOM and visible item-level `span` DOM, visible and hidden header ref-link anchors old `target=\"_blank\"` plus `rel=\"noopener\"` DOM, header metadata including old Eye icon view count and `mr-2` utility class, contributors, contest reference links including hidden-link caret trigger, hidden-link trigger inherited header text color and no extra CSS left margin, old Ant Design light/dark inherited text colors for non-link header metadata, old header ref-link light/dark primary colors and hover colors, old header export/share action no-`href` anchor DOM with old utility classes plus Ant Design runtime `ant-dropdown-trigger`, no `title` / `aria-label`, no `ant-btn` classes, no Vue-only action trigger/separator product classes, old header export/share action no-gap spacing and block/inline display, old header export/share action icon light/dark primary colors and hover colors, old header time format, Ant Design Vue icon export/share dropdown triggers, old header-to-progress spacing, old progress wrapper `mx-4` spacing, old conditional header action `pl-2 border-l` separators for meta and no-meta routes, old `导出为` export menu group, runtime `_now` cleanup for SRK/converter-backed exports, Ant Design share-copy success notifications and old copied-link URL behavior that removes focus-only query keys and URL fragments, old render-error plain `div role="alert"` wrapper with inline `max-width: 400px; margin: 100px auto`, no Vue-only `rankland-ranklist-error` product class, and Ant Design Alert role/icon/message/description, Ant Design Vue Select/Switch/Radio filter controls including old controls root `mt-3 mx-4 flex justify-between items-center`, old plain filters root with empty class list, organization Select old `ml-2` class plus inline `width: 160px` and no Vue-only `.rankland-ranklist-select` product class, official filter old `ml-5 inline-flex items-center` class list without Vue-only `.rankland-ranklist-checkbox`, official label `mr-1`, marker filter old `ml-5 inline-flex items-center` class list without Vue-only `.rankland-ranklist-marker-filter`, and old `ml-2`/`ml-5`/`mr-1` spacing, old progress-to-controls `mt-3` row spacing, old organization-filter `已选择 N 个` compact selected tag and unique organization options, hydration-safe filter reload behavior, collection selected-ranklist wrapper controls and old `pb-8` content class token, playground preview header, standalone `ml-4` table-wrapper offset for ranklist/live, old `mt-6` table top spacing and real table spacer DOM, old remarks wrapper `mb-4 text-center` utility classes, wrapper-local `.srk-remarks` pill styling with old light/dark primary rgba borders, low-level SRK table theme propagation, macOS Blink optimized table-header `box-sizing: content-box`, status-block score-details `margin-top: 0`, legal `result: null` status cells now render old React problem-alias fallback text without status-block classes, old `@algoux/standard-ranklist@0.3.12` / `@algoux/standard-ranklist-utils@0.2.13` dependency baseline, user modal legacy responsive width, user modal title/body behavior without duplicate body username, user modal dark body text color, organization line old `mb-0` class plus zero-margin spacing, team-members row old `mt-2` class plus spacing, raw ` / ` separator text, and item-level entry span DOM, non-official notice line old `mt-4 mb-0` class plus spacing, `所在奖区` segment line old `mt-4 mb-0` class plus zero-bottom spacing, `所在奖区` segment label old `.user-modal-segment-label` class plus style, `x_slogan` old `.slogan` class plus styling with old ZCOOL XiaoWei font assets, SRK preset marker label styling, old photo/slogan shared `mt-4` wrapper structure plus full-width photo styling, empty photo/slogan wrapper for users without photo/slogan, old chart-only rank-time `mt-4` wrapper class with no unit/summary/event chrome, old footer root `text-center mt-8` and paragraph `mb-0` / `mt-1 mb-0` utility classes, old footer paragraph spacing, old footer external package/project links `target=\"_blank\"` with no `rel`, footer contact trigger old no-`href` anchor DOM and link colors, conditional China-site beian footer, and old `@antv/g2` rank-time tooltip/animation behavior are restored. Remaining lower-level exact table pixel parity should be handled by product-review-driven slices.
- SRK header metadata product classes now match old React more closely: contributors render exact `mb-0`, ref-links render no class, and contest time renders exact `text-center mb-0`, with the stable `data-id` hooks preserving scoped styling and full-chain assertions.
- SRK progress wrapper now matches old React more closely: the wrapper renders exact `mx-4` without the Vue-only `rankland-ranklist-progress` product class, while stable `data-id` scoped styles preserve the 16px horizontal margins and mobile ProgressBar wrapping behavior.
- SRK controls root now matches old React more closely: the wrapper renders exact `mt-3 mx-4 flex justify-between items-center` without the Vue-only `rankland-ranklist-controls` product class, while stable `data-id` scoped styles preserve flex display, centered alignment, space-between justification, 12px top margin, 16px horizontal margins, and normal row/column gaps.
- SRK footer root now matches old React more closely: the wrapper renders as a plain `DIV` with exact `text-center mt-8` and without the Vue-only `rankland-ranklist-footer` product class, while stable `data-id` scoped styles preserve footer spacing, paragraph margins, footer link colors, ContactUs trigger behavior, and conditional beian behavior.
- SRK extra-action wrapper now matches old React more closely: the wrapper keeps `data-id="rankland-ranklist-extra-action"` but renders as a plain `div` with an empty class list, including the old always-present empty right-side controls `DIV` on normal Ranklist routes without an extra-action slot, while preserving block display, normal gaps, Live scroll-solution toggle behavior, and controls root chrome.
- SRK header actions wrapper now matches old React more closely: the wrapper keeps `data-id="rankland-ranklist-header-actions"` but no longer emits the Vue-only `rankland-ranklist-header-actions` product class, while data-id scoped styles preserve `display: inline`, normal gaps, old export/share trigger utility classes, Ant Design Vue dropdown behavior, and metadata/no-metadata separator chrome.
- SRK hidden reference-link trigger now matches old React more closely: the trigger keeps `data-id="rankland-ranklist-ref-link-extra-action"` for stable tests/styles, no longer emits the Vue-only `rankland-ranklist-ref-link-extra-action` product class, preserves Ant Design's runtime `ant-dropdown-trigger`, and keeps pointer cursor, zero left margin, inherited header text color, caret rendering, hover dropdown behavior, and hidden-link `target="_blank"` / `rel="noopener"` coverage.
- SRK header view-count now follows old React `meta && meta.viewCnt || '-'` behavior: metadata responses without `viewCnt` still render the Eye icon and `-` fallback, while routes that do not pass metadata continue to omit the view-count block.
- SRK header view-count now renders the old React exact `mr-2` class list while preserving the migrated `data-id="rankland-ranklist-view-count"` test hook and no longer emits Vue-only `.rankland-ranklist-view-count`.
- SRK hidden reference-link trigger now preserves old React's zero CSS left margin while keeping the pointer cursor, caret icon, inherited header text color, and hover dropdown behavior.
- SRK header meta/action row no longer adds Vue-only flex gap; spacing now comes from old `mr-2`, `pl-2`, and `border-l` utility classes.
- SRK header meta row now restores old block display while the migrated action hook behaves inline, matching old React inline action children.
- SRK header export/share triggers now render as old React no-`href` anchors instead of Ant Design Vue buttons; metadata routes keep `border-0 border-solid border-gray-400 mr-2 pl-2 border-l ant-dropdown-trigger` for export and `pl-2 border-0 border-l border-solid border-gray-400 ant-dropdown-trigger` for share, while live/no-metadata export keeps `border-0 border-solid border-gray-400 mr-2 ant-dropdown-trigger` without `pl-2 border-l`; the triggers also omit old-React-absent `title` and `aria-label` attributes, and export actions no longer render Vue-only inline `rankland-ranklist-action-status` text after downloads.
- SRK renderer normal path no longer emits the Vue-only `.rankland-ranklist` root wrapper or `header.rankland-ranklist-header`; Ranklist full-chain now asserts the old React direct top-level order under `ranklist-content` after the hidden hydration marker: banner wrapper, `h1`, metadata block, time paragraph, progress, controls, spacer, table wrapper, and footer.
- SRK banner image now restores old React's exact `mb-2` class plus inline `max-width: min(100%, 1820px)` / `max-height: 40vh` contract and removes the Vue-only `rankland-ranklist-banner` product class and scoped size CSS.
- SRK header contributors and reference links now render inside the old React `div.text-center.mt-1` metadata block instead of as Vue-only sibling nodes, while time remains the next top-level renderer sibling.
- SRK table area now renders the old React `div.mt-6` spacer DOM before the table wrapper without the Vue-only `.rankland-ranklist-table-spacer` product class while preserving the verified 24px controls-to-table visual gap.
- SRK table wrapper now preserves the old React class-attribute contract: ranklist/live render exact `class="ml-4"` from `tableClass`, while collection/playground keep the stable `data-id` test hook without emitting a Vue-only class attribute.
- SRK user and solution modal wrapper nodes now render inside the table wrapper like old React `StyledRanklistRenderer`, while the footer remains after the table wrapper.
- User modal wrap and body now render closer to old React: the user modal `.srk-modal-wrap` no longer carries the Vue-only `rankland-user-modal` wrap class, and the body renders the old React exact `.user-modal` root class without the Vue-only body class while preserving child hooks and dark text color coverage.
- User modal organization line now renders the old React exact `mb-0` class token without Vue-only organization-line classes while preserving the stable `data-id` hook and computed zero-margin coverage.
- User modal team-members row now renders the old React exact `user-modal-info-team-members mt-2` class tokens without the Vue-only team-members row class while preserving the stable `data-id` hook, item-level entry spans, raw separator text, opacity, padding, and computed spacing coverage.
- User modal team-members separator now renders the old React exact `user-modal-info-team-members-slash` class token without the Vue-only separator class while preserving stable `data-id`, raw ` / ` text, normalized slash text, item-level member entry DOM, and separator style coverage.
- User modal team members now render old React item-level outer `span` entries, preserving the migrated member/separator hooks while avoiding Vue-only flattened children.
- User modal markers row now renders the old React exact `user-modal-info-markers mt-2` class tokens without the Vue-only marker-row class while preserving marker labels, preset marker classes, label styling, and computed spacing coverage.
- User modal marker label now renders the old React modal class plus SRK preset class, with no Vue-only marker-label class, while preserving the stable `data-id`, marker text, preset class/style, inline-block layout, and computed spacing/style coverage.
- User modal unofficial line now renders the old React exact `mt-4 mb-0` class tokens without the Vue-only unofficial-line class while preserving the stable `data-id` hook and computed spacing coverage.
- User modal segment line now renders the old React exact `mt-4 mb-0` class tokens without Vue-only segment-line classes while preserving the stable `data-id` hook, segment label, and computed spacing coverage.
- User modal segment label now renders the old React exact `user-modal-segment-label bg-segment-*` class tokens without the Vue-only segment-label class while preserving the stable `data-id` hook and existing style/background coverage.
- User modal slogan now renders the old React exact `slogan mt-4 mb-2` class tokens without the Vue-only slogan class while preserving the stable `data-id` hook, shared photo/slogan wrapper, and computed typography/spacing coverage.
- User modal rank-time panel now renders the old React exact `mt-4` wrapper class token without the Vue-only rank-time panel class while preserving the stable `data-id` hook, chart-only G2 rendering, and existing computed spacing coverage.
- User modal rank-time chart now keeps the old React plain RankCurve outer wrapper and inner G2 container contract by removing Vue-only `rankland-rank-time-curve` / `rankland-rank-time-g2-chart` classes while preserving stable `data-id` hooks, G2 metadata/canvas coverage, relative wrapper layout, and 400px chart height.
- User modal rank-time chart wrapper now also omits the Vue-only `data-chart-status` lifecycle attribute and removes stale shared `.rankland-rank-time-curve` CSS, keeping the old plain RankCurve wrapper closer to React while preserving accepted test hooks and G2 coverage.
- User modal photo wrapper now renders the old React exact `mt-4` class token without the Vue-only `.rankland-user-modal-photo` hook while preserving existing full-width image coverage.
- User modal photo and slogan now share the old React exact `div.mt-4` wrapper structure without the Vue-only `.rankland-user-modal-photo` hook while preserving old `.slogan mt-4 mb-2` classes and full-width image coverage.
- User modal users without photo or slogan now still render the old empty photo/slogan `div.mt-4` wrapper, matching React `UserInfoModal` spacing and DOM parity.
- User modal photo now restores the old React inline `width: 100%` style on the image while preserving the existing computed full-width CSS and broken-image hiding behavior.
- SRK user and solution modal roots now carry the old React `srk-react-modal-root` class alongside `srk-modal-root`, `srk-animated-modal-root`, and `srk-general-modal-root`, and the user modal wrap omits the Vue-only `rankland-user-modal` class, preserving old modal root/wrap DOM hooks without changing the Vue renderer modal behavior.
- User modal users without organization now still render the old empty organization `p.mb-0` line before the rest of the modal body, matching React `UserInfoModal` DOM parity while preserving existing zero-margin styling.
- SRK wrapper now runs the old `ts-interface-checker@1.0.2` Ranklist checker before static conversion and renders the old checker-error exact `div.ml-8 > h3 + pre` DOM/class for structurally invalid SRK objects without the Vue-only `.rankland-ranklist-check-error` product class, while deterministic mock fixtures now use checker-valid marker presets.
- App shell header navigation now restores the old React middle wrapper `DIV` between the logo link and site-switch area, with inline/computed `flex: 1 1 0%` and `min-width: 0px`; full-chain coverage verifies `[data-id="app-nav"]` lives inside that wrapper and is no longer a direct child of `.app-header-inner`, while desktop/mobile bounds remain green.
- App shell right menu now restores the old React `RightMenu` plain outer `DIV` as the third `.app-header-inner` direct child; full-chain coverage verifies it has no class/inline style, contains `[data-id="app-site-switch"]`, and keeps the site-switch button out of the header's direct-child list while preserving desktop/mobile bounds.
- Live route state wrappers now restore old React plain `DIV.mt-16.text-center` DOM for NotFound, generic error, and loading states; full-chain coverage verifies the Vue-only `live-state` class is gone, loading wraps the Ant Design Spin as a child instead of using Spin as the state root, and normal live desktop/mobile bounds remain green.
- Ranklist route state wrappers now restore old React plain `DIV.mt-16.text-center` DOM for NotFound, generic error, and loading states; focused coverage verifies the Vue-only `ranklist-state` class is gone, loading wraps the Ant Design Spin as a child instead of using Spin as the state root, and ranklist desktop/mobile bounds remain green.
- Collection route state wrappers now restore old React plain `DIV.pt-16.text-center` DOM for collection-level NotFound/error/loading and selected-ranklist error/loading states; focused coverage verifies the Vue-only `collection-state` class is gone, loading wraps the Ant Design Spin as a child instead of using Spin as the state root, and collection desktop/mobile bounds remain green.

## Known Risks

- Route foundations preserve core route compatibility and audited full-chain behavior, but do not claim pixel-perfect parity with old React/Ant Design pages.
- Vue app shell now preserves the legacy chrome with Ant Design Vue Layout/Menu/Dropdown/Button/BackTop, old root `layout` class, old header inner `flex justify-between` classes with computed `space-between`, old header middle nav wrapper `DIV` with inline/computed `flex: 1 1 0%` and `min-width: 0px`, old header right-menu plain `DIV` wrapper as the third `.app-header-inner` direct child with no class/inline style around `[data-id="app-site-switch"]`, old inner `.logo` 64px box and 40px logo image, old `ArrowRightOutlined` site-switch external-link icon, old site-switch trigger `px-2` class token with 8px horizontal padding, old site-switch link `target="_blank"` with no `rel`, old site-switch current URL filtering that drops `focus` / `聚焦` and hash fragments, old site-switch dropdown branch style where the China branch keeps `word-break: keep-all` and the `cnn` global branch renders no inline word-break, old title/subtitle direct `p.mb-0` paragraph class lists with no Vue-only `app-site-switch-title` / `app-site-switch-subtitle` product classes, old subtitle span `opacity-60 text-xs` class tokens with 12px/0.6 presentation, old nav menu 46px line-height and dark primary `#f6ac06` selected/active/hover color plus underline, old site-switch 32px button height/0px min-height/2px radius and 8px horizontal padding, old desktop 50px header padding with no centered max-width/no inner gap, old mobile 20px header padding/no inner padding/no inner gap, old mobile 64px header/logo container, 40px logo image, 16px nav item padding, 8px site-switch padding, old light-mode `#f2f2f2` body background, old global body text color light `rgba(0, 0, 0, 0.85)` and dark `rgba(255, 255, 255, 0.85)`, old global body typography 14px/system-font/tabular-nums/1.5715 line-height, old shared Ant Design contact modal dark surface/title/close/body padding, old ContactUs no-`href` anchor trigger DOM, old contact QQ image `w-full` class, focus-mode bypass, old document head defaults including `lang="zh-Hans"`, legacy viewport, SEO/OpenGraph/Twitter meta, Dark Reader lock, and fallback title, old body pre-hydration opacity/background bootstrap and Dark Reader cleanup before app mount, hydrated body opacity release to `1`, pre-hydration theme bootstrap, system theme class sync, macOS Blink optimization class, GA initialization/pageview dispatch, fallback 404 copy/status/spacing, and desktop/mobile viewport bounds. The Home page restores the old `main.normal-content` / `.home-intro` wrapper DOM, restores the old `div.block` content group DOM, restores the old `normal-content` desktop/mobile padding, removes the newer desktop max-width cap, restores old `home-intro` block spacing, restores the old hero paragraph `text-base` class token, restores old recommendation/tool rows to horizontal-only gutter with no extra row gap, restores old recommendation/tool card column `mb-4` class tokens with 16px bottom spacing, restores old tool-card logo `mr-3 inline-block` class tokens with inline-block display, restores old about separator `mx-2` class token with 8px left/right spacing, restores old `h1.block-title` section headings, restores old nullish statistics fallback, restores total SRK count `strong` presentation, restores default visible external links as old `target="_blank"` anchors without `rel`, restores the conditional beian link as the old `target="_blank"` anchor without `rel`, and restores the old Ant Design Card/Row/Col recommendation and tool layout through Ant Design Vue, including recommendation card `h2` headings, `UnorderedListOutlined`/`TrophyOutlined` icons, old `mr-3` title icon/logo spacing, old card paragraph `mt-4 mb-0` spacing, and the old paste.then.ac logo padding, with row gutter margins constrained to avoid SSR/hydration first-frame horizontal overflow. The Ant Design Vue Menu is intentionally client-only because its ResizeObserver overflow wrapper causes SSR/client hydration node mismatches when rendered server-side. Analytics uses a lightweight local gtag adapter instead of `react-ga4`; full-chain E2E verifies dispatch intent through an E2E-only probe while external Google requests remain denied.
- Collection navigation now uses Ant Design Vue inline Menu, Ant Design Vue `MenuFoldOutlined`/`MenuUnfoldOutlined` collapse icons, category logo assets, old loaded-state Head `og:title`, absolute `og:url`, and absolute canonical URL, old loaded route root `DIV`, old loaded shell `DIV.srk-collection-container` with no Vue-only root `position` / `min-height` chrome and computed `position: static` / `min-height: 0px`, old nav `DIV.srk-collection-nav`, old ranklist leaf menu label anchors with collection `rankId` href and no Vue-only `role` / `aria-current`, old directory menu label spans, selected-path submenu auto-open plus manual close interaction coverage, old collapse button plain wrapper `DIV` with no class/inline style and the button as its direct child, old hidden header `DIV.srk-collection-hidden-header`, old panel `DIV.srk-collection-ranklist`, old selected-ranklist content `DIV.pb-8` with 32px bottom padding, no Vue-only `collection-page` / `collection-nav` / `collection-hidden-header` / `collection-ranklist-panel` / `collection-ranklist-content` product classes, old collection-level NotFound/error/loading and selected-ranklist error/loading exact plain `DIV.pt-16.text-center` DOM with no Vue-only `collection-state` class and loading Spin as a child, old collapsed submenu-title 0px horizontal padding, old `.srk-collection-ranklist` `position: relative` and `flex: 1`, old nav `z-index: 1`, hidden-header `z-index: auto`, old light `#f4f4f4` and dark `#111111` nav background plus dark `#434343` border, old `.srk-collection-hidden-header h3.mb-0` hidden-title DOM and title style contract with no inline `marginTop` / no Vue-only `line-height: 1`, persisted collapse state, mobile selected-ranklist collapse behavior, old `useRemainingHeight`-style body/header height calculation, menu height as remaining height minus 40px, and nav-width-driven button/header/panel animation styles. The Ant Design Vue Menu is intentionally client-only because its ResizeObserver overflow wrapper also causes SSR/client hydration node mismatches in inline mode.
- Ranklist, collection, and live Not Found states now preserve the old Ant Design primary small Back-to-Home button treatment while retaining stable route-level `data-id` selectors; ranklist NotFound/error/loading wrappers preserve exact old plain `DIV.mt-16.text-center` DOM, and collection-level NotFound/error/loading plus selected-ranklist error/loading wrappers preserve exact old plain `DIV.pt-16.text-center` DOM, remove Vue-only state product classes, and keep loading Spin as a child.
- Playground now restores the old route-local Head contract by omitting canonical links while preserving title and `og:title`, restores the old one-time welcome modal keyed by `PlaygroundWelcomeMessageRead`, including the old right-top `QuestionCircleOutlined` docs cue copy, uses Ant Design Vue Tag/Modal for the shortcut cue and welcome modal, mounts a client-side Monaco JSON editor through `@guolao/vue-monaco-editor@1.6.0` and `monaco-editor@0.43.0`, serves `/monaco-editor/vs` locally from Koa, configures SRK schema diagnostics, syncs dark/light Monaco theme, restores Monaco's default minimap chrome by preserving only the old React option `selectOnLineNumbers: true`, restores the old `/playground` route wrapper as a plain `DIV` with no Vue-only `.playground-page` class and no route-local `70vh` min-height, restores the old route-level plain no-class client wrapper after the hidden hydration marker, restores the old exact `DIV.srk-playground-container` core container as a direct child of that wrapper and without Vue-only `.playground-layout` class, restores the old `.srk-playground-container` desktop flex container with 500px editor and old `.srk-playground-preview` / `flex: 1` preview pane, removes the Vue-only editor wrapper border/radius, removes the Vue-only page-level `Playground` toolbar/heading and visible Preview button, hides internal hydration and editor-ready probes from product UI while preserving full-chain selectors, restores the old preview-pane `div.absolute.right-4.top-4` wrapper with exact old class `absolute right-4 top-4` around the plain no-class `QuestionCircleOutlined` srk docs anchor with `target="_blank"` and no `rel`, restores the old invalid JSON prompt without parser-detail behavior, `h3.mt-16.text-center` spacing/classes, and shortcut Tag `mr-0` class token plus 0px margin, restores the old valid SRK preview `div.mt-8.mb-8` wrapper without extra row-count metadata, restores the old preview header/title/time and `showFilter` wrapper path, restores the default SRK progress bar path, and keeps loader configuration out of SSR import side effects. Exact old Monaco `0.34.0` package-version parity and synthetic Monaco editing through Playwright remain deferred; synchronous `editor.setValue()` hangs in the current Vite 2 full-chain harness, so invalid/render-error states use an E2E-only preview hook while the product path uses Monaco `@change` and `Ctrl/Cmd + S`. Playground screenshot review uses no-animation viewport captures and a fresh mobile page because Chromium full-page screenshots and same-page post-screenshot renavigation can hang after Monaco plus SRK progress rendering in the full-chain browser process. Mobile keeps a no-horizontal-overflow column guard rather than reproducing old fixed-width overflow.
- Live realtime behavior has deterministic success, old route-local Head canonical omission coverage, hidden hydration marker coverage, NotFound, legacy `DIV.mt-16.text-center` state wrapper DOM for NotFound/error/loading with no Vue-only `live-state` class and loading Spin as a child, old loaded root `DIV` with no Vue-only `live-page` class and no route-local `70vh` min-height, old loaded content `DIV` wrapper with exact `mt-8 mb-8` class list, no Vue-only `live-content` / `live-content-with-scroll-solution` product classes, 32px top/bottom spacing, old inline `250px` scroll-solution offset, and old mobile realtime content-left `250px`, old loaded wrapper chrome with no route padding/no width cap/no auto-centering plus old `250px` scroll-solution left offset, WebSocket error reconnect, unexpected WebSocket close reconnect, Ant Design Vue scroll-solution Switch shape/state coverage including old `inline-flex items-center` toggle wrapper classes, old `mr-1` text span class, old 4px text-to-switch spacing from text margin with normal wrapper gap, old 250ms `URLSearchParams(window.location.search)` plus `window.location.search` browser reload behavior for scroll-solution toggle changes, old plain extra-action wrapper with empty class list plus normal Ranklist empty right-side controls wrapper parity, and old 14px / light `rgba(0, 0, 0, 0.85)` toggle text style, old mobile no-toggle-DOM behavior below 768px, old unknown-result fallback with `--` text and no extra `result-unknown` class, old Toastify `newestOnTop={false}` oldest-first visible row order, scroll-solution toggle reload disable path with preserved query params and `beforeunload` coverage, hidden internal status marker, desktop/mobile realtime layout bounds, legacy Toastify container classes, old Toastify `DIV` container/direct visible `DIV` row DOM with no `ul` / `li` product structure, Toastify row shape, hidden close/progress affordances, `Toastify__zoomIn` animation coverage, normal live desktop/mobile page review, and mobile progress label bounds coverage. Reconnect uses bounded exponential backoff at 1000 ms, 2000 ms, 4000 ms, 8000 ms, then 10000 ms.
- SRK renderer wrapper is shared by multiple migrated routes. It now removes the Vue-only `.rankland-ranklist` root wrapper and `header.rankland-ranklist-header`, covers the old React direct top-level child order, has deterministic header banner wrapper `flex items-center justify-center` without Vue-only `.rankland-ranklist-banner-wrap`, preserves `display:flex`, `align-items:center`, and `justify-content:center`, banner image `mb-2`, title `text-center mb-1`, meta row exact `text-center mt-1` without Vue-only `.rankland-ranklist-header-meta`, contributors `mb-0`, ref-links, time `text-center mb-0`, old contributors/ref-links meta-block child DOM coverage, old broken SRK banner/user-photo image hiding coverage, header title 32px/500/4px typography coverage, header metadata/contributors/ref-links/time 14px text-size coverage, old contributor visible item-level `span` DOM coverage, old contributor link `target=\"_blank\"` plus `rel=\"noopener\"` coverage, old ref-links `span` wrapper DOM and visible item-level `span` DOM coverage, old visible and hidden ref-link `target=\"_blank\"` plus `rel=\"noopener\"` coverage, contributor/ref-link fixture coverage, Eye icon view-count coverage including exact old `mr-2` class with no Vue-only `.rankland-ranklist-view-count`, hidden ref-link caret trigger coverage, old hidden ref-link trigger inherited header text color and zero CSS left-margin coverage, old header meta/action no extra flex-gap coverage, old header meta/action block/inline display coverage, old Ant Design light/dark inherited text color coverage for non-link header metadata, old header ref-link and footer contact trigger color/hover plus footer contact trigger anchor DOM coverage, old header export/share action no-`href` anchor DOM with old utility classes plus Ant Design runtime `ant-dropdown-trigger`, no `ant-btn` classes, no Vue-only action trigger/separator product classes, old header export/share action icon light/dark primary and hover color coverage, old header-to-progress spacing coverage, old progress wrapper `mx-4` spacing coverage, Ant Design Vue header action icon coverage, old conditional header action separator coverage for meta and no-meta routes, old export menu group coverage, runtime `_now` cleanup coverage for SRK/converter-backed exports, share-copy Ant Design notification coverage plus copied-link hash omission coverage, old render-error wrapper DOM/class parity + render-error Ant Design Alert role/icon/message/description/centered-spacing coverage, Ant Design Vue filter-control coverage including old controls root `mt-3 mx-4 flex justify-between items-center`, the always-present empty right-side controls `DIV` for routes without an extra-action slot, Select `ml-2`, official filter `ml-5 inline-flex items-center` without Vue-only `.rankland-ranklist-checkbox`, official label `mr-1`, marker filter `ml-5 inline-flex items-center`, and old `ml-2`/`ml-5`/`mr-1` spacing, old progress-to-controls `mt-3` spacing coverage, old organization-filter compact tag and unique options coverage, hydration-safe filter reload coverage, old official-only filtering semantics, standalone ranklist/live table-wrapper offset coverage, old `div.mt-6` table spacer coverage without Vue-only `.rankland-ranklist-table-spacer`, collection non-offset coverage, old remarks wrapper `mb-4 text-center` class coverage, wrapper-local remarks pill and old primary rgba border coverage, low-level table dark theme propagation coverage, macOS Blink optimized table-header box-model coverage, status-block score-details spacing coverage, old SRK dependency baseline unit coverage, user modal legacy responsive width coverage, user modal title-only username coverage, user modal dark body text color coverage, user modal organization-line old `mb-0` class plus zero-margin coverage including empty organization users, user modal team-members row old `mt-2` class plus spacing, raw ` / ` separator text, and item-level entry span DOM coverage, non-official notice line old `mt-4 mb-0` class plus spacing coverage, segment-line old `mt-4 mb-0` class plus spacing coverage, segment label old `.user-modal-segment-label` class plus style, slogan old `.slogan` class plus style and ZCOOL XiaoWei font-face load coverage, SRK preset marker label, old photo/slogan shared wrapper `mt-4` structure plus 16px top margin and full-width photo coverage, empty photo/slogan wrapper coverage for users without photo/slogan, old chart-only rank-time wrapper class with 16px top margin and no unit/summary/event chrome coverage, old footer root `text-center mt-8` and paragraph `mb-0` / `mt-1 mb-0` utility class coverage, old footer paragraph spacing coverage, old footer external package/project links `target=\"_blank\"` with no `rel` coverage, old footer ContactUs no-`href` anchor trigger DOM coverage, conditional China-site beian footer state coverage, route-level content spacing coverage across ranklist, collection, and live, route-level full-chain tests across ranklist, collection, live, and playground, and G2 rank-time chart model/full-chain coverage for tooltip and animation metadata. Remaining parity changes should stay isolated and heavily tested.
- SRK banner image class/style parity now specifically covers exact old `mb-2`, inline `max-width: min(100%, 1820px)` / `max-height: 40vh`, and absence of the Vue-only `rankland-ranklist-banner` product class or scoped size CSS.
- SRK remarks wrapper exact-class parity now covers exact old `mb-4 text-center`, no Vue-only `rankland-ranklist-remarks` product class, and preserved 16px bottom margin / centered alignment / `.srk-remarks` pill styling.
- Collection nav collapse button now restores the old React plain wrapper `DIV` before the Ant Design button; focused full-chain coverage verifies the wrapper has no class/inline style, the button remains its direct child, and collection desktop/mobile bounds stay green.
- Collection menu ranklist leaves now restore old React `Link` anchor DOM with collection `rankId` href and no Vue-only `role` / `aria-current`; focused full-chain coverage verifies directory labels remain `SPAN`, menu click/mobile collapse behavior stays green, and collection desktop/mobile bounds stay green.
- Collection menu open-key interaction is now covered: focused full-chain coverage verifies the selected `dir-icpc` submenu auto-opens for `rankId=test-key`, can be manually closed by clicking the submenu title, hides the selected leaf, and keeps the URL on `/collection/official?rankId=test-key`; no production code change was needed because current behavior already matched.
- Ranklist full-chain coverage now asserts old loaded-state Head `og:title`, absolute `og:url`, and absolute canonical URL plus old top-level SRK renderer direct-child DOM without Vue-only `.rankland-ranklist` / `.rankland-ranklist-header` wrappers, old contributor visible item-level `span` DOM, old contributor link `target=\"_blank\"` plus `rel=\"noopener\"`, old ref-links `span` wrapper DOM and visible item-level `span` DOM, old visible and hidden ref-link `target=\"_blank\"` plus `rel=\"noopener\"`, old footer GitHub/Standard Ranklist/榜单合集 external link `target=\"_blank\"` plus omitted `rel` DOM, old footer ContactUs no-`href` anchor trigger DOM, exact old view-count `mr-2` class list with no Vue-only `.rankland-ranklist-view-count`, export/share no-`href` anchor triggers with old utility classes plus `ant-dropdown-trigger`, without `title` / `aria-label`, without Ant Design button/Vue-only action classes, and without Vue-only inline `rankland-ranklist-action-status` text after export downloads, hidden ref-link trigger zero CSS left-margin, header meta/action no extra flex-gap, header meta/action block/inline display, old table spacer `mt-6` DOM, old progress wrapper `mx-4` spacing plus the old user and solution modal wrapper ancestry and root classes, old `.user-modal` body root class, old `mb-0` organization line class, old `mt-2` team-members row class, old team separator raw ` / ` text, old item-level team-member entry span DOM, old `mt-2` markers row class, old `mt-4 mb-0` unofficial and segment line classes, old `.user-modal-segment-label` segment label class, old `.slogan mt-4 mb-2` slogan classes, exact old photo wrapper `mt-4` class with no Vue-only `.rankland-user-modal-photo`, old photo/slogan shared-parent DOM structure, and old empty photo/slogan wrapper for users without photo/slogan in addition to the existing user-modal title/body, dark text, organization, team-member, marker, photo, segment, slogan typography/font/spacing, and rank-time parity checks. Playground full-chain now also covers old empty organization `p.mb-0` DOM parity for users without organization.
- Converter-backed SRK exports use lazy browser imports of `@algoux/standard-ranklist-convert-to@0.2.2`; `xlsx@0.18.5` remains a large but click-loaded dependency.
- User modal rank-time parity now dynamically imports `@antv/g2@5.0.13` in the browser to match the old React `RankCurve` tooltip and animation behavior, restores the modal chrome to the old chart-only `mt-4` wrapper class with no extra unit, summary, or solved-event chips, and restores the old plain RankCurve outer/inner chart container contract by omitting Vue-only `rankland-rank-time-curve` / `rankland-rank-time-g2-chart` class tokens and Vue-only `data-chart-status` lifecycle DOM while keeping stable test hooks and 400px chart layout. This restores product parity at the cost of a large lazy client chunk for first chart open.

## Next Slice Queue

1. Route-level product polish after manual/product review.
