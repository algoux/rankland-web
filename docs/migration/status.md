# RankLand Migration Status

This file is the quick global dashboard for the RankLand migration. Update it at the end of every verified slice or when a slice's state materially changes.

## Current Focus

- Active branch: `migration/live-page-foundation`
- Current slice: global body typography parity
- Latest slice commit: `fix: 还原全局正文排版基线`
- Last recorded full gate: passed build, 35 unit files / 151 unit tests, 1 SSR smoke test, 1 shallow Playwright test, and 54 full-chain Playwright tests across all migrated public routes during 2026-05-27 global body typography parity verification; focused app-shell RED/GREEN also passed; focused ranklist inherited-typography follow-up also passed; `git diff --check` also passed
- Next recommended focus: product-review-driven SRK lower-level table pixel parity or route polish

## Route Progress

| Route | Render | Status | Branch / Slice | Coverage | Remaining parity / risk |
| --- | --- | --- | --- | --- | --- |
| `/` | SSR | Foundation + app shell + legacy main.normal-content/home-intro wrapper DOM parity + legacy `div.block` content group DOM parity + legacy normal-content/block spacing parity + legacy `h1.block-title` section heading parity + legacy statistics nullish fallback parity + legacy statistics `strong` typography parity + Ant Design Vue shell/content product parity + legacy global body text color/typography parity + legacy home non-card dark text color parity + legacy contact modal Ant Design parity + legacy home card dark Ant Design parity + legacy header box model parity + legacy nav menu line-height/primary-color parity + legacy site-switch button parity + legacy mobile shell metrics parity + legacy recommendation card heading/icon parity + legacy card title `mr-3` spacing parity + legacy card paragraph `mt-4 mb-0` spacing parity + legacy paste.then.ac logo padding parity + analytics parity + legacy title separator parity + visual review + completion audit verified | `migration/live-page-foundation` | SSR smoke, shallow E2E, full-chain E2E, app shell/theme/bootstrap/light-background/global body light/dark text color plus old 14px/system-font/tabular-nums/1.5715 line-height typography coverage, analytics/site-switch ArrowRight icon/full-chain title-navigation E2E, app shell legacy desktop 50px header padding/no centered max-width/no inner gap, app shell legacy nav menu 46px line-height and dark primary `#f6ac06` selected/underline color, app shell legacy site-switch 32px height/0px min-height/2px border-radius, app shell mobile legacy 20px header padding/no inner padding/no inner gap, 64px header/logo container, 40px logo image, 16px nav item padding, and 8px site-switch padding coverage, Ant Design Vue shell DOM coverage, legacy home `main.normal-content` and `.home-intro` wrapper DOM coverage, legacy home `div.block` content group DOM coverage, legacy home normal-content desktop 32px/50px padding, mobile 32px/20px padding, no max-width cap, block 40px top spacing, block title 20px bottom spacing, `h1.block-title` DOM, 32px/500 section heading typography coverage, partial upstream statistics `-` fallback coverage across SSR/hydration, and total SRK count `strong` DOM plus bold/non-italic presentation coverage, Ant Design Vue home Card/Row/Col coverage including old dark card background `#141414`, border `#303030`, radius 2px, and inherited text color coverage, old home non-card hero/resource/about text dark color coverage, old recommendation card `h2` plus `UnorderedListOutlined`/`TrophyOutlined` icon coverage, old card title icon/logo `mr-3` right-spacing coverage, old card paragraph `mt-4 mb-0` DOM plus 16px/0px margin coverage, old paste.then.ac logo 24px/2px padding coverage, old contact modal Ant Design dark content/title/close/body padding coverage, app shell desktop/mobile screenshot review, home desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/search` | CSR | Foundation + legacy normal-content layout parity + Ant Design Input.Search/List/Spin/default search icon button parity + legacy list/empty-state spacing/dark text parity + legacy load-error color parity + hydration marker visual parity + legacy row metadata typography parity + legacy h3 heading parity + zero-result display parity + view-count Eye icon parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, generated route, full-chain E2E including legacy normal-content wrapper/padding coverage, legacy h3 heading coverage, hidden hydration marker coverage, Ant Design search control/list/loading coverage, old boolean `enterButton` default SearchOutlined icon button coverage, legacy load-error message/40px spacing/`text-red-500` color coverage, legacy `mt-2` list and recent empty-state spacing plus dark text color coverage, legacy `opacity-70` section/view-count and `mb-0 opacity-50 text-sm` created-at coverage, zero-result no-extra-empty-state coverage, and recent/result view-count Eye icon coverage, search desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/ranklist/:id` | SSR | Foundation + Not Found Ant Design action parity + generic load error action parity + loading spinner parity + route content spacing parity + shared wrapper header title typography/header metadata text-size/view-count icon/meta-to-contributors spacing/icon hover dropdown/action icon primary-color/action separator/export-group/caret/share notification/header-to-progress/link primary-color/extra ref-link trigger color/header meta text color parity + Ant Design Vue filter controls/organization selected-tag/unique options/internal spacing parity + controls-row spacing parity + table-wrapper offset/top-spacing parity + SRK remarks pill placement/style/legacy primary border color parity + SRK table theme/table-header box model/status-block score-details spacing parity + old SRK dependency baseline parity + user modal title/body/dark text color/responsive-width/organization line/team members/unofficial line/segment line/segment label/slogan/font asset/marker label/photo width parity + runtime `_now` export cleanup parity + footer paragraph/footer beian parity + G2 rank-time tooltip/animation parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E including NotFound Ant Design primary/small return button, generic ranklist load error `mt-16 text-center` and Ant Design primary small Refresh coverage, legacy Ant Design Spin loading fallback with `mt-16 text-center` state styling unit coverage, old SRK header title 32px/500/4px typography coverage, old SRK header view-count/contributors/ref-links/time 14px text-size coverage, old Eye icon view count, contributors, reference links, hidden reference-link caret trigger coverage, old hidden reference-link trigger inherited header text color coverage, old header meta/view-count/ref-link-label/time light `rgba(0, 0, 0, 0.85)` and dark `rgba(255, 255, 255, 0.85)` text color coverage, old header ref-link and footer contact trigger light `#ff8104` / dark `#f6ac06` color plus light `#ff9d2e` / dark `#a7770b` hover coverage, old header export/share action icon light `#ff8104` / dark `#f6ac06` color plus light `#ff9d2e` / dark `#a7770b` hover coverage, old header meta-to-contributors 0px spacing coverage, route `mt-8 mb-8` spacing coverage, old header-to-progress spacing coverage, Ant Design Vue Download/Share icon trigger coverage, old `pl-2 border-l` header action separator coverage, old `导出为` export menu group coverage, runtime `_now` cleanup for SRK/converter-backed exports, Ant Design copy-link/copy-embed success notification coverage, hover export/share dropdown open-close, Ant Design Vue Select/Switch/Radio filter controls and official-only semantics, old filter-control `ml-2`/`ml-5`/`mr-1` spacing coverage, old progress-to-controls `mt-3` spacing coverage, old organization-filter `已选择 N 个` compact selected tag and unique options coverage, hydration-safe filter reload coverage, standalone table-wrapper `ml-4` offset coverage, old SRK table-wrapper `mt-6` top-spacing coverage, wrapper-local `.srk-remarks` pill style plus light `rgba(255, 129, 4, 0.8)` and dark `rgba(246, 172, 6, 0.8)` border coverage, low-level SRK dark theme problem-header coverage, macOS Blink optimized low-level SRK table-header `box-sizing: content-box` coverage, SRK status-block score-details `margin-top: 0` coverage, old SRK dependency baseline unit coverage, user modal title-only username/no duplicate body heading coverage, user modal dark body text color `rgba(255, 255, 255, 0.85)` coverage, user modal legacy responsive width coverage, user modal organization-line zero-margin coverage, user modal team-member row, non-official notice line, `所在奖区` segment line mt-4/mb-0 coverage, `所在奖区` segment label, `x_slogan` style and ZCOOL XiaoWei font-face load coverage, SRK preset marker label, full-width photo coverage, old footer paragraph `mt-1 mb-0` spacing coverage, conditional China-site beian footer state, G2 rank-time chart metadata/canvas coverage, footer contact modal, ranklist desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/collection/:id` | SSR | Foundation + Not Found Ant Design action parity + generic load error action parity + loading spinner parity + Ant Design Vue menu/category icon/mobile collapse/collapse-icon product parity + legacy collapsed submenu padding parity + legacy ranklist panel layout parity + legacy shell class/nav z-index parity + legacy nav chrome color/hidden-header DOM parity + exact remaining-height/pixel animation parity + empty-selection heading parity + selected-ranklist loading/action/error parity + selected-ranklist shared wrapper/content spacing/view-count icon parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E, NotFound Ant Design primary/small return button, generic collection load error `pt-16 text-center` and Ant Design primary small Refresh coverage, legacy Ant Design Spin loading fallback with `pt-16 text-center` state styling unit coverage, selected-ranklist switching/mismatch Ant Design Spin with `pt-16 text-center` unit coverage, Ant Design Vue `MenuFoldOutlined`/`MenuUnfoldOutlined` collapse icon component coverage, layout helper coverage, Ant Design Vue menu/category icon/mobile collapse coverage, old collapsed submenu-title `padding-left: 0px` and `padding-right: 0px` coverage, old `.srk-collection-ranklist` `position: relative` and `flex: 1` coverage, old `.srk-collection-container` / `.srk-collection-nav` / `.srk-collection-ranklist` shell class, old nav `z-index: 1`, and hidden-header `z-index: auto` coverage, old nav light `#f4f4f4` / dark `#111111` background and dark `#434343` border coverage, old `.srk-collection-hidden-header h3.mb-0` DOM coverage, legacy remaining-height/nav animation contract coverage, empty-selection `h3.pt-16.text-center` coverage, selected-ranklist error `pt-16 text-center` and Ant Design primary small Refresh coverage, selected-ranklist wrapper header/filter/progress/footer/action coverage, selected-ranklist Eye icon view count, selected-ranklist `pb-8` spacing coverage, collection desktop/mobile screenshot review, `test:migration` | Product polish after route parity review |
| `/playground` | CSR | Foundation + hidden hydration marker parity + Ant Design Vue welcome modal/action + Monaco editor/schema/theme/remaining-height product parity + invalid JSON prompt/spacing parity + preview docs-link/icon placement + preview SRK header/filter/progress/wrapper/render-error Alert parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E, hidden hydration marker coverage, one-time welcome modal/localStorage coverage, Monaco readiness/schema/theme coverage, old preview-pane `QuestionCircleOutlined` srk docs link placement coverage, invalid JSON prompt without parser-detail plus old `mt-16 text-center` spacing coverage, shared Ant Design Vue preview header/title/time/filter/progress coverage, old Ant Design render-error Alert role/icon/message/description/400px centered spacing coverage, old `mt-8 mb-8` preview wrapper spacing and no extra row-count metadata coverage, deterministic no-animation playground desktop/mobile viewport screenshot review, `test:migration` | Exact old Monaco `0.34.0` package-version parity and synthetic Monaco editing in Playwright remain deferred risk; product editor path is verified by hydration/build and stable preview coverage |
| `/live/:id` | CSR | Foundation + hidden hydration marker parity + parity follow-ups + Not Found Ant Design action parity + generic load error action parity + loading spinner parity + legacy state spacing parity + route content spacing parity + reconnect/backoff + Toastify animation/pixel parity + shared Ant Design Vue filter/share notification controls + Ant Design Vue scroll-solution Switch parity + legacy scroll toggle spacing/text style parity + conditional no-meta header action separator + table-wrapper offset + runtime `_now` export cleanup parity + G2 rank-time tooltip/animation parity + visual review + completion audit verified | `migration/live-page-foundation` | Unit, route, full-chain E2E including hidden hydration marker coverage, NotFound Ant Design primary/small return button, generic live load error fixed-message/no-debug-detail and Ant Design primary small Refresh coverage, legacy Ant Design Spin loading coverage while live info is loading, legacy `mt-16 text-center` state spacing coverage, WebSocket error reconnect, unexpected WebSocket close reconnect, Ant Design Vue scroll-solution Switch coverage, old 4px scroll-solution toggle text-to-switch spacing coverage, old 14px and light `rgba(0, 0, 0, 0.85)` scroll-solution toggle text style coverage, scroll-solution toggle close, mobile toggle visibility, hidden internal status marker, desktop/mobile realtime layout bounds, route `mt-8 mb-8` spacing coverage, legacy Toastify container/Zoom presentation, shared Ant Design Vue filter interaction, no-meta export/share header action separator coverage, runtime `_now` cleanup for SRK/converter-backed exports, shared copy-embed success notification coverage, standalone table-wrapper `ml-4` offset coverage, user modal G2 rank-time chart metadata/canvas coverage, normal live desktop/mobile screenshot review, mobile progress label bounds, and `test:migration` | Product polish after route parity review |
| `/:catchAll(.*)` | CSR fallback | Legacy fallback 404 copy/status/spacing parity verified | `migration/live-page-foundation` | Full-chain E2E covers unknown public route HTTP 404 plus legacy `404 Not Found · 你来到了榜单荒地`, `mt-32`, `text-xl`, and centered layout | Product polish after route parity review |

## Infrastructure Progress

| Area | Status | Notes |
| --- | --- | --- |
| Node / package manager | Done | Node 24, pnpm 8 |
| RankLand API service | Done | Normal/CDN API adapters, cache behavior, error mapping |
| Route builders | Done | Public route builders and generated route coverage. `corepack pnpm run gen:client-router` now exits after one-shot generation; `gen:client-router:watch` preserves dev-time watch mode. |
| SRK Vue wrapper | Foundation + live controls + modal clicks + G2 rank-time user modal + user modal responsive width + user modal title/body/dark text color/organization line + user modal team members/unofficial line/segment line/segment label/slogan/font asset/marker label/photo width + asset URL rewriting + basic export/share + converter-backed exports with runtime `_now` cleanup + old export menu group + share-copy Ant Design notifications + render-error Ant Design Alert + footer contact prompt/link color + old footer paragraph spacing + conditional China-site beian footer + old header title 32px/500/4px typography + old header metadata/contributors/ref-links/time 14px text size + header metadata + old header metadata light/dark text colors + old header meta-to-contributors spacing + old header-to-progress spacing + old header time format + Eye icon view count + old header ref-link primary colors + old header export/share action icon primary and hover colors + old hidden ref-link trigger inherited text color + Ant Design Vue icon hover dropdowns/caret triggers + old conditional header action separators + Ant Design Vue filter controls/internal spacing + old organization-filter compact selected tag/unique options + hydration-safe filter reload coverage + old progress-to-controls `mt-3` spacing + collection selected-wrapper controls + playground preview header + standalone table-wrapper offset + old `mt-6` table top-spacing + wrapper-local remarks pill with old primary rgba border colors + low-level table theme propagation + optimized table-header box model + status-block score-details spacing + old `@algoux/standard-ranklist@0.3.12` / `@algoux/standard-ranklist-utils@0.2.13` dependency baseline + default preview progress done | Remaining lower-level exact table pixel parity is product/visual review driven |
| Full-chain E2E harness | Done | Real bwcx/Koa app plus controlled mock RankLand backend |
| Vue app shell | Foundation + Ant Design Vue product parity + legacy global body text color/typography parity + legacy contact modal Ant Design parity + legacy header box model parity + legacy nav menu line-height/primary-color parity + legacy site-switch button parity + legacy mobile metrics parity + theme sync + analytics + legacy title separator + fallback 404 + visual review verified | Logo, primary navigation, site switch dropdown, old `ArrowRightOutlined` external-link cue via Ant Design Vue, old desktop 50px header padding with no centered max-width/no inner gap, old nav menu 46px line-height, old dark nav primary `#f6ac06` selected/active/hover color and underline, old site-switch 32px button height/0px min-height/2px radius with 8px horizontal padding, old mobile 20px header padding/no inner padding/no inner gap, old mobile 64px header/logo container, 40px logo image, 16px nav item padding, 8px site-switch padding, old `Title \| RankLand` document-title separator on direct load and CSR navigation, old light-mode `#f2f2f2` body background, old global body text color light `rgba(0, 0, 0, 0.85)` and dark `rgba(255, 255, 255, 0.85)`, old global body typography 14px/system-font/tabular-nums/1.5715 line-height, old shared contact modal via Ant Design Vue Modal with dark content/title/close/body padding coverage, BackTop, legacy focus-mode shell bypass, pre-hydration theme bootstrap, system theme class sync, macOS Blink optimization class, GA initialization/pageview dispatch, legacy fallback 404 copy/status/spacing, desktop/mobile viewport bounds, and screenshot review are covered by full-chain E2E. Ant Design Vue `a-menu` is mounted client-only to avoid its ResizeObserver SSR hydration mismatch. |
| Migration process | Final review evidence refreshed | Slice workflow, conversation I/O protocol, this dashboard, completion audit, manual acceptance checklist, and final integration review are in `docs/migration` and `docs/superpowers`; 2026-05-27 docs now reflect the latest 35 unit files / 151 unit tests / 53 full-chain product-parity gate instead of the older route-compatible-only review |

## Deferred Product Decisions

- Home: old React `main.normal-content` and `.home-intro` wrapper DOM, old `div.block` content group DOM, `normal-content` desktop/mobile padding, no desktop max-width cap, `home-intro` block spacing, `h1.block-title` section headings, statistics `?? '-'` nullish fallback, total SRK count `strong` bold/non-italic presentation, Ant Design recommendation/tool card layout is restored with Ant Design Vue Card/Row/Col, including old dark Ant Design Card background/border/radius/text color, non-card hero/resource/about dark text color, recommendation card `h2` headings, `UnorderedListOutlined`/`TrophyOutlined` icons, old `mr-3` icon/logo right spacing, old card paragraph `mt-4 mb-0` 16px/0px spacing, and the paste.then.ac logo's old `24px` size plus `2px` padding, while preserving SSR statistics, canonical/OG/JSON-LD, shared Ant Design Vue contact modal, and app-shell viewport bounds.
- Fallback 404: old React `404 Not Found · 你来到了榜单荒地` copy, `mt-32 text-xl text-center` layout, and HTTP 404 status for unknown public routes are restored.
- Page titles: old React `formatTitle` behavior using `Title | RankLand` is restored across direct route loads and CSR navigation; empty titles still render `RankLand`.
- Ranklist: old React `Spin` loading fallback with `mt-16 text-center` layout is restored for the no-ranklist state; route-level asyncData still blocks initial client navigation until ranklist data is available.
- Search: old React `normal-content` wrapper/padding, `h3.mb-6` heading, no visible hydration/test marker, Ant Design `Input.Search` with default boolean `enterButton` SearchOutlined icon button, `List`, `Spin`, `EyeOutlined` view-count icon, load-error message with `mt-10` spacing and `text-red-500` color, `mt-2` list/recent-empty spacing, recent-empty dark text color, row metadata opacity/spacing typography, and zero-result summary-only behavior are restored for the search form, loading/error state, recent rows, and result rows.
- Collection: Ant Design Vue inline menu, category logo assets, old React `MenuFoldOutlined`/`MenuUnfoldOutlined` collapse icons via Ant Design Vue components, mobile collapse behavior, old collapsed submenu title 0px horizontal padding, old `.srk-collection-ranklist` `position: relative` and `flex: 1`, old React `Spin` loading fallback with `pt-16 text-center` layout, selected-ranklist switching/mismatch `Spin` loading state, empty-selection `h3.pt-16.text-center`, generic collection load error and selected-ranklist error `pt-16 text-center` with Ant Design primary small Refresh actions, old `.srk-collection-container` / `.srk-collection-nav` / `.srk-collection-ranklist` shell classes, old nav `z-index: 1`, hidden-header `z-index: auto`, old light/dark nav chrome colors, old `.srk-collection-hidden-header h3.mb-0` DOM, exact remaining-height calculation, and nav/menu/panel pixel animation contract are restored.
- Playground: hidden hydration marker behavior, one-time welcome modal, Ant Design Vue preview action, Monaco editor, SRK schema diagnostics, theme-aware editor theme, remaining-height behavior, old preview-pane `QuestionCircleOutlined` srk docs link placement, old invalid JSON prompt without parser-detail behavior and `mt-16 text-center` spacing, old preview header/title/time, old Ant Design render-error Alert role/icon/message/description and 400px centered spacing, old `mt-8 mb-8` preview wrapper without extra row-count metadata, old preview `showFilter` behavior, and default SRK progress bar behavior are restored. Exact old Monaco `0.34.0` package-version parity is intentionally not preserved because the verified Vue wrapper requires Monaco `>=0.43.0`.
- Live: hidden hydration marker behavior, Ant Design Vue scroll-solution Switch with old 4px text-to-switch spacing plus old 14px / light `rgba(0, 0, 0, 0.85)` toggle text style, generic load error fixed-message/no-debug-detail with Ant Design primary small Refresh action, legacy Ant Design Spin loading state, legacy `mt-16 text-center` state spacing, automatic WebSocket reconnect/backoff, and legacy Toastify container/Zoom animation behavior are restored as product enhancements.
- SRK renderer wrapper: header title old 32px/500/4px typography, header metadata/contributors/ref-links/time old 14px text size, header metadata including old Eye icon view count, contributors, contest reference links including hidden-link caret trigger, hidden-link trigger inherited header text color, old Ant Design light/dark inherited text colors for non-link header metadata, old header ref-link light/dark primary colors and hover colors, old header export/share action icon light/dark primary colors and hover colors, old header meta-to-contributors 0px spacing, old header time format, Ant Design Vue icon export/share dropdown triggers, old header-to-progress spacing, old conditional header action `pl-2 border-l` separators for meta and no-meta routes, old `导出为` export menu group, runtime `_now` cleanup for SRK/converter-backed exports, Ant Design share-copy success notifications, old Ant Design render-error Alert role/icon/message/description and centered 400px wrapper, Ant Design Vue Select/Switch/Radio filter controls including old `ml-2`/`ml-5`/`mr-1` spacing, old progress-to-controls `mt-3` row spacing, old organization-filter `已选择 N 个` compact selected tag and unique organization options, hydration-safe filter reload behavior, collection selected-ranklist wrapper controls, playground preview header, standalone `ml-4` table-wrapper offset for ranklist/live, old `mt-6` table top spacing, wrapper-local `.srk-remarks` pill styling with old light/dark primary rgba borders, low-level SRK table theme propagation, macOS Blink optimized table-header `box-sizing: content-box`, status-block score-details `margin-top: 0`, old `@algoux/standard-ranklist@0.3.12` / `@algoux/standard-ranklist-utils@0.2.13` dependency baseline, user modal legacy responsive width, user modal title/body behavior without duplicate body username, user modal dark body text color, zero-margin organization line, user modal team-member row, non-official notice line, zero-bottom `所在奖区` segment line, `所在奖区` segment label, `x_slogan` styling with old ZCOOL XiaoWei font assets, SRK preset marker label styling, full-width photo styling, old footer paragraph `mt-1 mb-0` spacing, footer contact trigger old link colors, conditional China-site beian footer, and old `@antv/g2` rank-time tooltip/animation behavior are restored. Remaining lower-level exact table pixel parity should be handled by product-review-driven slices.

## Known Risks

- Route foundations preserve core route compatibility and audited full-chain behavior, but do not claim pixel-perfect parity with old React/Ant Design pages.
- Vue app shell now preserves the legacy chrome with Ant Design Vue Layout/Menu/Dropdown/Button/BackTop, old `ArrowRightOutlined` site-switch external-link icon, old nav menu 46px line-height and dark primary `#f6ac06` selected/active/hover color plus underline, old site-switch 32px button height/0px min-height/2px radius and 8px horizontal padding, old desktop 50px header padding with no centered max-width/no inner gap, old mobile 20px header padding/no inner padding/no inner gap, old mobile 64px header/logo container, 40px logo image, 16px nav item padding, 8px site-switch padding, old light-mode `#f2f2f2` body background, old global body text color light `rgba(0, 0, 0, 0.85)` and dark `rgba(255, 255, 255, 0.85)`, old global body typography 14px/system-font/tabular-nums/1.5715 line-height, old shared Ant Design contact modal dark surface/title/close/body padding, focus-mode bypass, pre-hydration theme bootstrap, system theme class sync, macOS Blink optimization class, GA initialization/pageview dispatch, fallback 404 copy/status/spacing, and desktop/mobile viewport bounds. The Home page restores the old `main.normal-content` / `.home-intro` wrapper DOM, restores the old `div.block` content group DOM, restores the old `normal-content` desktop/mobile padding, removes the newer desktop max-width cap, restores old `home-intro` block spacing, restores old `h1.block-title` section headings, restores old nullish statistics fallback, restores total SRK count `strong` presentation, and restores the old Ant Design Card/Row/Col recommendation and tool layout through Ant Design Vue, including recommendation card `h2` headings, `UnorderedListOutlined`/`TrophyOutlined` icons, old `mr-3` title icon/logo spacing, old card paragraph `mt-4 mb-0` spacing, and the old paste.then.ac logo padding, with row gutter margins constrained to avoid SSR/hydration first-frame horizontal overflow. The Ant Design Vue Menu is intentionally client-only because its ResizeObserver overflow wrapper causes SSR/client hydration node mismatches when rendered server-side. Analytics uses a lightweight local gtag adapter instead of `react-ga4`; full-chain E2E verifies dispatch intent through an E2E-only probe while external Google requests remain denied.
- Collection navigation now uses Ant Design Vue inline Menu, Ant Design Vue `MenuFoldOutlined`/`MenuUnfoldOutlined` collapse icons, category logo assets, old collapsed submenu-title 0px horizontal padding, old `.srk-collection-ranklist` `position: relative` and `flex: 1`, old `.srk-collection-container` / `.srk-collection-nav` / `.srk-collection-ranklist` shell classes, old nav `z-index: 1`, hidden-header `z-index: auto`, old light `#f4f4f4` and dark `#111111` nav background plus dark `#434343` border, old `.srk-collection-hidden-header h3.mb-0` hidden-title DOM, persisted collapse state, mobile selected-ranklist collapse behavior, old `useRemainingHeight`-style body/header height calculation, menu height as remaining height minus 40px, and nav-width-driven button/header/panel animation styles. The Ant Design Vue Menu is intentionally client-only because its ResizeObserver overflow wrapper also causes SSR/client hydration node mismatches in inline mode.
- Ranklist, collection, and live Not Found states now preserve the old Ant Design primary small Back-to-Home button treatment while retaining stable route-level `data-id` selectors.
- Playground now restores the old one-time welcome modal keyed by `PlaygroundWelcomeMessageRead`, uses Ant Design Vue Button/Tag/Modal for the visible editor action and shortcut cue, mounts a client-side Monaco JSON editor through `@guolao/vue-monaco-editor@1.6.0` and `monaco-editor@0.43.0`, serves `/monaco-editor/vs` locally from Koa, configures SRK schema diagnostics, syncs dark/light Monaco theme, restores the old preview-pane `QuestionCircleOutlined` srk docs link placement, restores the old invalid JSON prompt without parser-detail behavior and `mt-16 text-center` spacing, restores the old preview header/title/time, old `mt-8 mb-8` preview wrapper without extra row-count metadata, and `showFilter` wrapper path, restores the default SRK progress bar path, and keeps loader configuration out of SSR import side effects. Synthetic Monaco editing through Playwright and synchronous `editor.setValue()` both hang in the current Vite 2 full-chain harness, so invalid/render-error states use an E2E-only preview hook while the product path uses Monaco `@change`, Preview, and `Ctrl/Cmd + S`. Playground screenshot review uses no-animation viewport captures and a fresh mobile page because Chromium full-page screenshots and same-page post-screenshot renavigation can hang after Monaco plus SRK progress rendering in the full-chain browser process.
- Live realtime behavior has deterministic success, hidden hydration marker coverage, NotFound, legacy `mt-16 text-center` state spacing, WebSocket error reconnect, unexpected WebSocket close reconnect, Ant Design Vue scroll-solution Switch shape/state coverage including old 4px text-to-switch spacing and old 14px / light `rgba(0, 0, 0, 0.85)` toggle text style, scroll-solution toggle close without reconnect, mobile toggle visibility, hidden internal status marker, desktop/mobile realtime layout bounds, legacy Toastify container classes, Toastify row shape, hidden close/progress affordances, `Toastify__zoomIn` animation coverage, normal live desktop/mobile page review, and mobile progress label bounds coverage. Reconnect uses bounded exponential backoff at 1000 ms, 2000 ms, 4000 ms, 8000 ms, then 10000 ms.
- SRK renderer wrapper is shared by multiple migrated routes. It now has deterministic header title 32px/500/4px typography coverage, header metadata/contributors/ref-links/time 14px text-size coverage, contributor/ref-link fixture coverage, Eye icon view-count coverage, hidden ref-link caret trigger coverage, old hidden ref-link trigger inherited text color coverage, old Ant Design light/dark inherited text color coverage for non-link header metadata, old header ref-link and footer contact trigger color/hover coverage, old header export/share action icon light/dark primary and hover color coverage, old header meta-to-contributors spacing coverage, old header-to-progress spacing coverage, Ant Design Vue header action icon coverage, old conditional header action separator coverage for meta and no-meta routes, old export menu group coverage, runtime `_now` cleanup coverage for SRK/converter-backed exports, share-copy Ant Design notification coverage, old render-error Ant Design Alert role/icon/message/description/centered-spacing coverage, Ant Design Vue filter-control coverage including old `ml-2`/`ml-5`/`mr-1` spacing, old progress-to-controls `mt-3` spacing coverage, old organization-filter compact tag and unique options coverage, hydration-safe filter reload coverage, old official-only filtering semantics, standalone ranklist/live table-wrapper offset coverage, old table-wrapper mt-6 top-spacing coverage, collection non-offset coverage, wrapper-local remarks pill and old primary rgba border coverage, low-level table dark theme propagation coverage, macOS Blink optimized table-header box-model coverage, status-block score-details spacing coverage, old SRK dependency baseline unit coverage, user modal legacy responsive width coverage, user modal title-only username coverage, user modal dark body text color coverage, user modal organization-line zero-margin coverage, user modal team-member row, non-official notice line, segment-line mt-4/mb-0 coverage, segment label, slogan style and ZCOOL XiaoWei font-face load coverage, SRK preset marker label, full-width photo coverage, old footer paragraph `mt-1 mb-0` spacing coverage, conditional China-site beian footer state coverage, route-level content spacing coverage across ranklist, collection, and live, route-level full-chain tests across ranklist, collection, live, and playground, and G2 rank-time chart model/full-chain coverage for tooltip and animation metadata. Remaining parity changes should stay isolated and heavily tested.
- Converter-backed SRK exports use lazy browser imports of `@algoux/standard-ranklist-convert-to@0.2.2`; `xlsx@0.18.5` remains a large but click-loaded dependency.
- User modal rank-time parity now dynamically imports `@antv/g2@5.0.13` in the browser to match the old React `RankCurve` tooltip and animation behavior. This restores product parity at the cost of a large lazy client chunk for first chart open.

## Next Slice Queue

1. Product-review-driven SRK lower-level table pixel parity, if route review requires it.
2. Route-level product polish after manual review.
