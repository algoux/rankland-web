# RankLand Final Integration Review

Date: 2026-05-28
Branch: `migration/live-page-foundation`

This review records the evidence used to treat `rankland-web` as the product-restored migrated RankLand frontend for the currently verified public surface. It supersedes the earlier 2026-05-23 route-compatible-only review: subsequent slices restored Ant Design Vue shell/content parity, App shell legacy layout/logo class parity, analytics dispatch, fallback 404 class token parity, contact QQ image class parity, ContactUs trigger anchor DOM parity, App shell site-switch link `rel` omission parity, App shell site-switch dropdown content class/style parity, App shell site-switch trigger `px-2` class parity, Home external link `rel` omission parity, Beian link `rel` omission parity, Search canonical link omission parity, Playground canonical link omission parity, Live canonical link omission parity, Playground docs link `rel` omission parity, Playground docs link wrapper DOM/class parity, Playground docs link plain-anchor class parity, Playground valid preview wrapper DOM/class parity, Playground Monaco minimap/default option chrome parity, SRK table wrapper class attribute parity, search shell DOM parity, search heading DOM parity, search input DOM parity, search list item DOM parity, search result count DOM parity, search keyword whitespace parity, search state wrapper DOM parity, search section content DOM parity, search row content DOM parity, search error message DOM parity, search state/list utility class parity, search error DOM parity, SSR hydration marker visual parity for ranklist and collection, route state utility class parity for ranklist/live/collection NotFound/error/loading states, collection layout behavior including selected-ranklist `pb-8` class parity, loaded wrapper DOM parity, and hidden-header title style parity, Search product behavior including old route-local Head without canonical output, Playground product behavior including old shell classes, old desktop flex layout, editor shell chrome cleanup, old Monaco minimap/default option chrome, welcome modal right-top docs icon cue, invalid prompt h3 class and shortcut tag `mr-0` class-token spacing, old SRK checker-error exact `ml-8` wrapper class parity, old SRK render-error plain alert wrapper DOM/class parity, removed Vue-only toolbar/preview-action chrome, old route-local Head without canonical output, and hidden internal probes, Ranklist/Live route content utility class parity, Ranklist loaded wrapper DOM parity, Live route-level Head without canonical output, Live route wrapper chrome, Live root wrapper DOM/min-height parity, Live content wrapper DOM parity, Live reconnect/Toastify behavior, Live realtime Toastify `DIV` container/direct `DIV` row DOM parity, Live scroll-solution old visible row order, Live unknown-result class fallback, Live scroll toggle utility-class and `mr-1` spacing parity, Live mobile scroll toggle no-DOM parity, SRK wrapper actions/layout/modals including old modal root class parity and modal table-wrapper DOM parity, SRK renderer top-level wrapper DOM parity, SRK render-error wrapper DOM/class parity, SRK banner wrapper/image class parity, SRK remarks wrapper exact class parity, SRK header meta class parity, SRK view-count class parity, SRK header action anchor parity, SRK header action attribute parity, SRK header action status removal parity, SRK share link hash omission parity, SRK banner `mb-2` margin-source parity, SRK banner image inline style parity, SRK asset image error parity, header view-count fallback, hidden ref-link trigger spacing parity, contributor link `rel` parity, ref-links `span` wrapper and item-level `span` DOM parity, header ref-link `rel` parity, header meta DOM parity, footer external link `rel` omission parity, header utility class parity, header action utility class parity, header action gap parity, header action display parity, progress wrapper utility class parity, table spacer DOM parity, controls utility class parity, controls extra-action no-gap chrome parity, remarks wrapper utility class parity, footer utility class parity, user-modal legacy root, organization line, empty organization line, team-members row, team separator raw text, team-member entry DOM, markers row, unofficial line, segment line, segment label, slogan spacing classes, photo wrapper class, photo/slogan shared wrapper DOM, empty photo wrapper, rank-time chart-only wrapper class chrome, rank-time chart wrapper class parity, and rank-time chart status attribute parity, plus multiple low-level SRK renderer visual details.

The review still does not claim that every possible production SRK fixture has been pixel-reviewed against old React. Remaining lower-level table pixel parity is product-review-driven and should stay isolated in follow-up slices if new differences are found.

## Requirement Evidence

| Requirement | Evidence | Status |
| --- | --- | --- |
| Use Node 24 and pnpm 8 | `package.json` engines require Node `^24.0.0` and pnpm `^8.0.0`; local verification used `node v24.11.1` and `pnpm 8.15.9`. | Verified |
| Preserve public routes | `docs/migration/inventory.md`, `src/common/rankland-router/routes.ts`, `src/common/router/client-routes.ts`, and `src/client/router/routes.ts` all include `/`, `/search`, `/ranklist/:id`, `/collection/:id`, `/playground`, and `/live/:id`; scaffold `/about` and `/demo/detail/:id` are excluded from generated public route maps and covered as fallback 404s. | Verified |
| Preserve SSR/CSR boundary | `ranklandRoutes` marks `/`, `/ranklist/:id`, and `/collection/:id` as SSR; `/search`, `/playground`, and `/live/:id` as CSR. Generated route metadata uses `RenderMethodKind.SSR` for SSR routes and `undefined` for CSR routes. | Verified |
| Do not embed React | Migrated public route targets are Vue modules under `src/client/modules/**`; SRK rendering uses Vue/component-core packages and Vue wrapper code. | Verified |
| Do not hand-edit generated router outputs | Generated files retain the `DO NOT MODIFY IT BY HAND` header; `corepack pnpm run gen:client-router` is the one-shot generation gate and `gen:client-router:watch` is the dev watcher. | Verified |
| Keep RanklandApiService semantics | `src/common/rankland-api/rankland-api.service.ts` implements normal/CDN endpoints, NotFound mapping, cache reads/writes/deletes, and live ranklist methods; `tests/unit/rankland-api.service.spec.ts` covers endpoint URLs, cache behavior, and error mapping. | Verified |
| Keep API contract docs aligned | `docs/migration/api-contract.md` lists the same normal/CDN API methods and NotFound/error mapping covered by `RanklandApiService` tests. | Verified |
| Keep route inventory aligned | `docs/migration/inventory.md` maps each old React public route source to its Vue target module and render method. | Verified |
| Full-chain coverage for all public routes | `tests/e2e/full-chain/home.spec.ts`, `search.spec.ts`, `ranklist.spec.ts`, `collection.spec.ts`, `playground.spec.ts`, and `live.spec.ts` cover the migrated routes against the real app server and mock RankLand backend. | Verified |
| App shell/layout parity | `tests/e2e/full-chain/app-shell.spec.ts` covers Ant Design Vue Layout/Menu/Dropdown/Button shell structure, old root `layout` class, old header inner `flex justify-between` classes with computed `justify-content: space-between`, old inner `.logo` box at 64px by 64px, 40px logo image, navigation, old global body light/dark text colors, old global body 14px/system-font/tabular-nums/1.5715 line-height typography, old document body inline bootstrap style before `<body>`, Dark Reader cleanup before app mount, hydrated body opacity release to `1`, old nav menu 46px line-height, old dark primary nav selected/active/hover color and underline, site switch with old ArrowRight icon, old site-switch trigger `px-2` class token plus 8px computed left/right padding, old site-switch link `target="_blank"` with omitted `rel`, old site-switch dropdown link inline/computed `word-break: keep-all`, title/subtitle paragraph `mb-0` class tokens with zero bottom margin, subtitle span `opacity-60 text-xs` class tokens with 12px font size and 0.6 opacity, old site-switch 32px button height/0px min-height/2px radius and 8px horizontal padding, desktop legacy 50px header padding/no centered max-width/no inner gap, mobile legacy 20px header padding/no inner padding/no inner gap, 64px header/logo container, 40px logo image, 16px nav item padding, focus-mode shell bypass, old static document head defaults, pre-hydration theme bootstrap, theme/macOS class sync, legacy `Title \| RankLand` document-title behavior on CSR navigation, analytics dispatch, fallback 404 copy/status plus old `text-center mt-32 text-xl` class tokens, and desktop/mobile viewport bounds. Home full-chain coverage now also verifies old `main.normal-content` and `.home-intro` wrapper DOM, old `div.block` content group DOM, old `normal-content` padding, no desktop max-width cap, old `home-intro` block spacing, old hero title inline `font-size: 32px` plus computed 32px, old hero paragraph `text-base` class token, old recommendation/tool card row horizontal-only gutter with `rowGap: normal`, old recommendation/tool card column `mb-4` class tokens plus 16px bottom spacing, old tool logo `mr-3 inline-block` class tokens plus inline-block display and 12px right spacing, old about separator `mx-2` class token plus 8px left/right spacing, old `h1.block-title` section heading DOM/typography, old statistics nullish `-` fallback across SSR/hydration, old total SRK count `strong` DOM and bold/non-italic presentation, old default visible external links as `target="_blank"` anchors with omitted `rel`, conditional old beian link as a `target="_blank"` anchor with configured text and omitted `rel`, old card paragraph `mt-4 mb-0` 16px/0px spacing, old dark Ant Design Card background, border, radius, inherited text color, and old dark non-card body text color for hero/resource/about copy. | Verified |
| App document head meta parity | `tests/e2e/full-chain/app-shell.spec.ts` fetches raw HTML before hydration and verifies the old React document defaults: `lang="zh-Hans"`, legacy viewport, RankLand description, keywords, color-scheme, Dark Reader lock, Open Graph site metadata, Twitter card, fallback `RankLand` title, and existing pre-hydration theme bootstrap. | Verified |
| App document body bootstrap parity | `tests/e2e/full-chain/app-shell.spec.ts` fetches raw HTML and verifies old inline body bootstrap style before body, Dark Reader cleanup before app mount, and hydrated body opacity `1`. | Verified |
| Contact modal parity foundation | `src/client/components/contact-us.vue` is shared by home/ranklist footer surfaces and now uses ant-design-vue `Modal`; full-chain tests cover old clickable `span` plus no-`href` anchor trigger DOM on Home and SRK footer, open/close, email, QQ image, old QQ image `w-full` class, and old dark Ant Design modal content/title/close/body padding styles. | Verified |
| Search page parity | `tests/e2e/full-chain/search.spec.ts` covers CSR list-all loading, old route-local Head with no canonical link, hidden hydration marker, old root `div.normal-content`, plain inner `div`, no Vue-only `.search-page`, no `section.search-panel`, no route-local `min-height: 70vh`, old exact `h3.mb-6` heading class with no `.search-heading`, old search input no-page-custom-class DOM with no `.search-input` and 0px wrapper top margin, old result/recent Ant Design list item containers with no `.search-list-item` and `display:flex`, old loading/error/result/recent state wrappers using only `mt-10`, result/recent `DIV` wrappers, no Vue-only `.search-state` / `.search-section`, result/recent title nodes as old `div.opacity-70` with no `.search-section-title`, result summary exact old plain text `搜索到 N 个结果` with no Vue-only `[data-id="search-result-count"]` span while preserving section `data-result-count`, old keyword whitespace passthrough behavior for direct route queries, submitted search URLs, input value, whitespace-only result state, and local Fuse search, list spacing as old `div.mt-2 > .ant-list` with no `.search-list`, recent empty state as old `div.mt-2` with no `.search-empty-state`, result/recent row content exact old `p.mb-0`, `span.ml-2.opacity-70`, and `p.mb-0.opacity-50.text-sm` classes with no `.search-row-title`, `.search-view-count`, or `.search-created-at`, load-error inner message exact old `div.text-red-500` class with no `.search-error-message`, old `normal-content` padding and inherited text color, `h3.mb-6`, Ant Design Vue `Input.Search` with old boolean `enterButton` default SearchOutlined icon button, `List`/`Spin`, local Fuse search, empty `kw`, zero-result summary-only behavior, old loading `mt-10` Spin class token, old error outer `mt-10` wrapper and inner `text-red-500` message DOM, old result/recent `mt-10`, `opacity-70`, `mt-2`, `mb-0`, `ml-2`, `opacity-50`, and `text-sm` utility class tokens, row metadata typography, old recent-empty dark text color, and old load-error message/40px spacing/`text-red-500` color. | Verified |
| Collection page parity | `tests/e2e/full-chain/collection.spec.ts` covers SSR/hydration, hidden hydration marker, loaded root `DIV`, loaded shell `DIV.srk-collection-container`, loaded shell root computed `position: static` / `min-height: 0px` with no Vue-only root chrome, nav `DIV.srk-collection-nav`, hidden header `DIV.srk-collection-hidden-header`, hidden header title `h3.mb-0` with no inline `marginTop`, old expanded `marginLeft: 8px`, collapsed `fontSize: 14px` / `marginLeft: 0px`, and no Vue-only `line-height: 1`, ranklist panel `DIV.srk-collection-ranklist`, selected-ranklist old `DIV.pb-8` exact class list plus 32px bottom padding, absence of Vue-only wrapper product classes, Ant Design Vue menu/category icon/mobile collapse behavior, old shell/nav/hidden-header classes, remaining-height/nav animation contract, selected-ranklist wrapper/filter/progress/footer/action, collection empty-selection old `h3.pt-16.text-center` class tokens, collection-level NotFound/error `pt-16 text-center` route-state utility classes, selected-ranklist error `pt-16 text-center`, NotFound `h3.mb-4`, screenshots, and viewport bounds. | Verified |
| Collection container chrome parity | `tests/e2e/full-chain/collection.spec.ts` verifies `[data-id="collection-content"]` remains the old `DIV.srk-collection-container` while removing Vue-only root `position` / `min-height` chrome; computed CSS is `position: static` and `min-height: 0px`, with nav, hidden header, ranklist panel, remaining-height, and mobile collapse contracts still covered by the same full-chain file. | Verified |
| Collection Head full URL parity | `tests/e2e/full-chain/collection.spec.ts` covers the loaded `/collection/:id?rankId=...` Head contract from old React: title `Test Contest 2024 - 榜单合集 | RankLand`, `og:title`, absolute `og:url`, and absolute canonical URL using `https://rl.algoux.org/collection/official?rankId=test-key`. | Verified |
| SSR route hydration marker parity | `tests/e2e/full-chain/ranklist.spec.ts` and `tests/e2e/full-chain/collection.spec.ts` verify that `ranklist-hydrated` and `collection-hydrated` still expose `hydrated` text for tests while rendering as hidden 1px transparent probes instead of product-visible text. | Verified |
| Route state utility class parity | `tests/e2e/full-chain/ranklist.spec.ts`, `tests/e2e/full-chain/live.spec.ts`, `tests/e2e/full-chain/collection.spec.ts`, `tests/unit/ranklist-loading.spec.ts`, `tests/unit/collection-loading.spec.ts`, and `tests/unit/collection-ranklist-loading.spec.ts` cover old route-state class tokens: ranklist/live NotFound, generic error, and loading states carry `mt-16 text-center`; collection-level NotFound/error/loading plus selected-ranklist error/loading carry `pt-16 text-center`; ranklist/live/collection NotFound headings carry `mb-4`. | Verified |
| Ranklist page loaded wrapper parity | `tests/e2e/full-chain/ranklist.spec.ts` covers loaded `/ranklist/:id` root as old React plain `DIV`, loaded content wrapper as old React plain `DIV`, exact content class list `mt-8 mb-8`, no Vue-only `ranklist-content` product class, and preserved 32px top/bottom margins. | Verified |
| Playground page parity | `tests/e2e/full-chain/playground.spec.ts` covers CSR hydration, old route-local Head with no canonical link, old `.srk-playground-container` / `.srk-playground-preview` shell classes, old desktop flex layout with 500px editor and growing preview pane, no Vue-only editor wrapper border/radius, absence of the Vue-only page-level `Playground` toolbar/heading, absence of the Vue-only visible `Preview` button, hidden hydration marker, hidden editor-ready marker, Monaco readiness/schema/theme, old Monaco minimap/default option chrome with `.minimap` visible and nonzero-width while only old `selectOnLineNumbers: true` is passed, one-time welcome modal including old right-top `QuestionCircleOutlined` docs cue copy, preview docs link wrapper `div.absolute.right-4.top-4` with exact class `absolute right-4 top-4` and no Vue-only wrapper class, plain no-class docs anchor, icon placement, `target="_blank"`, and omitted `rel`, valid SRK preview wrapper `div.mt-8.mb-8`, invalid JSON state, old invalid prompt `h3.mt-16.text-center`, shortcut tag `mr-0` class token plus 0px right/left spacing, old checker-error exact `div.ml-8 > h3 + pre` DOM/class with no Vue-only `.rankland-ranklist-check-error` product class, render-error Alert/wrapper DOM parity, bundled preview rendering, shared SRK user modal empty organization line DOM parity through a no-organization preview fixture, desktop/mobile viewport bounds, and no upstream calls. | Verified |
| SRK renderer wrapper migrated behavior | `src/client/components/rankland-ranklist.vue` and related helpers cover Ant Design Vue filter/progress/header actions, old `ts-interface-checker` SRK validation before static conversion, old checker-error exact `div.ml-8 > h3 + pre` DOM/class with no Vue-only `.rankland-ranklist-check-error` product class, old render-error plain `div role="alert"` wrapper with inline `max-width: 400px; margin: 100px auto`, no Vue-only `rankland-ranklist-error` product class, and Ant Design Alert role/icon/message/description, old progress wrapper `mx-4` class and 16px horizontal margins, old header banner wrapper `flex items-center justify-center` with no Vue-only `.rankland-ranklist-banner-wrap`, computed `display:flex`, `align-items:center`, and `justify-content:center`, banner image exact old `mb-2` class plus inline `max-width: min(100%, 1820px)` / `max-height: 40vh` with no Vue-only `rankland-ranklist-banner` product class, title `text-center mb-1`, meta row `text-center mt-1`, contributors `mb-0`, ref-links, time `text-center mb-0`, old contributors/ref-links as children of the meta block, old broken SRK banner/user-photo image hiding behavior, old export action `border-0 border-solid border-gray-400 mr-2` plus conditional `pl-2 border-l`, old share action `pl-2 border-0 border-l border-solid border-gray-400`, old export/share trigger omission of old-React-absent `title` and `aria-label`, old header action no-gap spacing and block/inline display, old controls root `mt-3 mx-4 flex justify-between items-center` with no Vue-only declared gap, old plain block extra-action wrapper chrome, Select `ml-2`, official filter `ml-5 inline-flex items-center`, official label `mr-1`, marker filter `ml-5 inline-flex items-center`, old header title 32px/500/4px typography, old header meta/contributors/ref-links/time 14px text size, old contributor visible item `span` DOM, old contributor link `target="_blank"` / `rel="noopener"` DOM, old ref-links `span` wrapper DOM and visible ref-link item `span` DOM, old visible and hidden ref-link `target="_blank"` / `rel="noopener"` DOM, old Eye icon view-count behavior including `mr-2` class and missing-`viewCnt` `-` fallback, old header non-link metadata light/dark text colors, old header/footer link primary colors and hover colors, old header export/share action icon primary and hover colors, hidden ref-link trigger inherited text color and zero CSS left margin, hover dropdowns, export/share actions, embed code, modal clicks, old user/solution modal root classes including `srk-react-modal-root`, user modal details including old `.user-modal` root class, old `mb-0` organization line class, old empty organization `p.mb-0` line for users without organization, old `mt-2` team-members row class, old team separator raw ` / ` text, old team-member entry DOM, old modal wrapper table-container DOM, old `mt-2` markers row class, old `mt-4 mb-0` unofficial line classes, old `mt-4 mb-0` award line classes, old `.user-modal-segment-label` award label class, old `.slogan mt-4 mb-2` slogan classes, old photo wrapper `mt-4` class, old photo/slogan shared wrapper DOM, old empty photo/slogan wrapper for users without photo/slogan, full-width photo, and old dark body text color, old chart-only rank-time `mt-4` wrapper class with no extra unit/summary/event chrome, G2 rank-time chart, asset URL rewriting, footer root `text-center mt-8`, footer paragraph `mb-0` / `mt-1 mb-0` classes, old footer GitHub/Standard Ranklist/榜单合集 external package/project links with `target="_blank"` and omitted `rel`, old footer ContactUs no-`href` anchor trigger DOM, footer/contact prompt, conditional beian footer plus old beian link `target="_blank"` and omitted `rel`, live controls, route spacing including loaded ranklist/live route content `mt-8 mb-8` class tokens and 32px top/bottom spacing, table wrapper class-attribute parity plus offset/top spacing, old `div.mt-6` spacer DOM with no Vue-only `.rankland-ranklist-table-spacer`, and old modal wrapper table-container DOM, remarks wrapper exact `mb-4 text-center` class list with no Vue-only `rankland-ranklist-remarks`, remarks pill styling with old light/dark primary rgba borders, dark-theme propagation, macOS Blink table-header override, and SRK status score spacing; unit and full-chain tests cover those behaviors. | Verified |
| SRK mobile controls layout parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the shared ranklist filter controls preserve old React mobile computed layout: controls remain `flex-direction: row` and `align-items: center`, the filter group stays row/center with no Vue-only `12px` gap, organization Select remains `160px`, and official/marker filters keep `20px` left margins. | Verified |
| SRK filter-control inner gap parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the shared ranklist filter controls no longer rely on Vue-only wrapper `gap`; organization and checkbox wrappers compute `column-gap: normal`, while local old utility spacing restores the actual visual gaps to 8px from `筛选` to Select and 4px from `仅正式参赛` to Switch. | Verified |
| SRK filter-controls DOM parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the shared ranklist filter controls restore old React direct-child DOM: filters root children begin with `SPAN`, organization Select root, official wrapper `SPAN`, and marker Radio.Group root; organization Select is parented directly by the filters root; official wrapper is a `SPAN` without `.rankland-ranklist-filter`; and direct `LABEL` wrappers are absent. | Verified |
| SRK filters root class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the shared ranklist filters root keeps old React's plain `div` class contract: `[data-id="rankland-ranklist-filters"]` has an empty class list, while the stable `data-id` selector preserves flex/wrap/center/zero-gap layout and existing filter behavior. | Verified |
| SRK organization Select class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the organization Select root keeps old React's `ml-2` class plus inline `width: 160px` contract and does not render the Vue-only `.rankland-ranklist-select` product class, while computed width, 8px left margin, selected-tag behavior, and filter behavior remain covered. | Verified |
| SRK marker filter class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the marker Radio.Group keeps old React's `ml-5 inline-flex items-center` class contract and does not render the Vue-only `.rankland-ranklist-marker-filter` product class, while computed 20px left margin, marker filtering, and mobile layout remain covered. | Verified |
| SRK official filter class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the official-only wrapper keeps old React's `span.ml-5.inline-flex.items-center` contract and does not render the Vue-only `.rankland-ranklist-checkbox` product class, while computed 20px left margin, official-only filtering, label-to-switch spacing, and mobile layout remain covered. | Verified |
| SRK table spacer class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the table spacer keeps old React's plain `div.mt-6` class contract and does not render the Vue-only `.rankland-ranklist-table-spacer` product class, while the 24px controls-to-table visual gap and table wrapper zero top margin remain covered. | Verified |
| SRK banner wrapper/image class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the banner wrapper keeps old React's `flex items-center justify-center` class contract, does not render the Vue-only `.rankland-ranklist-banner-wrap` product class, and preserves `display:flex`, centered alignment, 0px wrapper bottom margin, and 8px banner image bottom margin. It also verifies the banner image class list is exactly `mb-2`, the old inline `max-width: min(100%, 1820px)` / `max-height: 40vh` style is present, and the Vue-only `rankland-ranklist-banner` product class is absent. | Verified |
| SRK header meta class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the header metadata block keeps old React's exact `text-center mt-1` class contract, does not render the Vue-only `.rankland-ranklist-header-meta` product class, and preserves block display, no-gap spacing, child DOM, 14px text size, and 4px top margin. | Verified |
| SRK header meta product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that header contributors now render exact `mb-0`, ref-links render an empty class list, and contest time renders exact `text-center mb-0`, removing Vue-only `rankland-ranklist-contributors`, `rankland-ranklist-ref-links`, and `rankland-ranklist-time` while preserving text size, colors, child DOM, and link attributes. | Verified |
| SRK progress wrapper product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the progress wrapper now renders exact old React `mx-4`, removes Vue-only `rankland-ranklist-progress`, and preserves 16px horizontal margins plus existing ProgressBar behavior. | Verified |
| SRK controls wrapper product class parity | `tests/e2e/full-chain/ranklist.spec.ts` and `tests/e2e/full-chain/live.spec.ts` verify that the controls root now renders exact old React `mt-3 mx-4 flex justify-between items-center`, removes Vue-only `rankland-ranklist-controls`, and preserves flex display, centered alignment, space-between justification, 12px top margin, 16px horizontal margins, and normal row/column gaps. | Verified |
| SRK footer root DOM parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the footer root now renders old React `DIV.text-center.mt-8`, removes Vue-only `rankland-ranklist-footer`, and preserves footer spacing, paragraph margins, external links, ContactUs trigger behavior, link colors, conditional beian behavior, and SRK renderer top-level DOM order. | Verified |
| User modal organization line product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-organization"]` now renders exact old React `mb-0`, removes Vue-only `rankland-user-modal-line` / `rankland-user-modal-organization`, and preserves organization text plus zero top/bottom margins. | Verified |
| User modal team members product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-team-members"]` now renders exact old React `user-modal-info-team-members mt-2`, removes Vue-only `rankland-user-modal-team-members`, and preserves member text, raw separator text, item-level entry spans, opacity, padding, and 8px top margin. | Verified |
| User modal team separator product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-team-separator"]` now renders exact old React `user-modal-info-team-members-slash`, removes Vue-only `rankland-user-modal-team-separator`, and preserves raw ` / ` text, item-level team-member entry DOM, 0.5 opacity, and 80% font size. | Verified |
| User modal segment line product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-segment"]` now renders exact old React `mt-4 mb-0`, removes Vue-only `rankland-user-modal-line` / `rankland-user-modal-segment`, and preserves award-segment text, label, and 16px/0px margins. | Verified |
| User modal segment label product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-segment-label"]` now renders exact old React `user-modal-segment-label bg-segment-gold`, removes Vue-only `rankland-user-modal-segment-label`, and preserves label text, background class, inline-block display, 4px padding, 4px border radius, and white text. | Verified |
| User modal slogan product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-slogan"]` now renders exact old React `slogan mt-4 mb-2`, removes Vue-only `rankland-user-modal-slogan`, and preserves slogan text, shared photo/slogan wrapper, centered 32px ZCOOL XiaoWei typography, `SLOGAN` pseudo-label, 16px top margin, and 8px bottom margin. | Verified |
| User modal unofficial line product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-unofficial"]` now renders exact old React `mt-4 mb-0`, removes Vue-only `rankland-user-modal-unofficial`, and preserves notice text plus 16px/0px margins. | Verified |
| User modal body product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the user modal body root now renders exact old React `user-modal`, removes Vue-only `rankland-user-modal-body`, and preserves title/body behavior, child DOM hooks, photo width calculation, and modal body text color coverage. | Verified |
| User modal photo wrapper product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the shared photo/slogan wrapper now renders exact old React `mt-4`, removes Vue-only `rankland-user-modal-photo`, and preserves shared photo/slogan parent DOM, empty wrapper behavior, inline photo width, computed full-width/max-width behavior, and broken-image coverage. | Verified |
| SRK controls empty extra-action DOM parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that normal Ranklist controls preserve the old React always-present empty right-side `DIV[data-id="rankland-ranklist-extra-action"]` when no extra-action slot is provided; `tests/e2e/full-chain/live.spec.ts` verifies the populated Live extra-action wrapper still renders as the old React plain `div` with an empty class list, removes Vue-only `rankland-ranklist-extra-action`, and preserves block display, normal row/column gaps, Live scroll-solution toggle behavior, and controls root chrome. | Verified |
| SRK view-count class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the header view-count node keeps old React's exact `mr-2` class contract, does not render the Vue-only `.rankland-ranklist-view-count` product class, and preserves Eye icon, `42` / `-` fallback, 14px metadata text size, light/dark metadata color, and header-action separator behavior. | Verified |
| SRK header action anchor/attribute parity | `tests/e2e/full-chain/ranklist.spec.ts` and `tests/e2e/full-chain/live.spec.ts` verify that the export/share triggers render as old no-`href` `A` anchors, keep old utility class lists plus Ant Design runtime `ant-dropdown-trigger`, omit old-React-absent `title` and `aria-label` attributes, do not render `ant-btn` / `ant-btn-sm` or Vue-only `.rankland-ranklist-header-action-trigger` / `.rankland-ranklist-header-action-separated`, and preserve metadata/no-metadata separator behavior. | Verified |
| SRK header actions wrapper product class parity | `tests/e2e/full-chain/ranklist.spec.ts` and `tests/e2e/full-chain/live.spec.ts` verify that `[data-id="rankland-ranklist-header-actions"]` now preserves the stable wrapper hook but renders with an empty class list, removing Vue-only `rankland-ranklist-header-actions` while preserving `display: inline`, normal gaps, old export/share trigger utility classes, Ant Design Vue dropdown behavior, and metadata/no-metadata separator chrome. | Verified |
| SRK extra ref-link trigger product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-ranklist-ref-link-extra-action"]` now preserves the stable hook, removes the Vue-only `rankland-ranklist-ref-link-extra-action` product class, keeps Ant Design's runtime `ant-dropdown-trigger`, and preserves pointer cursor, zero left margin, inherited header text color, caret rendering, hover dropdown behavior, and hidden ref-link `target="_blank"` / `rel="noopener"` attributes. | Verified |
| SRK header action status removal parity | `tests/e2e/full-chain/ranklist.spec.ts` and `tests/e2e/full-chain/live.spec.ts` verify that SRK/Gym Ghost/VJudge Replay/Excel export downloads keep filename/content coverage while no Vue-only `[data-id="rankland-ranklist-action-status"]` node is rendered after export actions; share-copy success remains covered through Ant Design Vue notification assertions. | Verified |
| SRK share link hash omission parity | `tests/unit/rankland-ranklist-actions.spec.ts` verifies copied page links remove focus-only query keys and URL fragments while preserving other query params; `tests/e2e/full-chain/ranklist.spec.ts` verifies the real share menu copy-link action copies the old React-style URL without reloading ranklist data. | Verified |
| SRK renderer top-level wrapper DOM parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that the shared SRK renderer no longer emits Vue-only `.rankland-ranklist` or `.rankland-ranklist-header` wrappers, and that the normal Ranklist path renders old React direct top-level children under `ranklist-content` after the hidden hydration marker: banner wrapper, `h1`, metadata block, time paragraph, progress, controls, spacer, table wrapper, and footer. | Verified |
| Live realtime Toastify DOM parity | `tests/e2e/full-chain/live.spec.ts` verifies that the realtime submission panel now uses the old React Toastify DOM shape: `DIV` container, no `ul` / `li` product structure, and direct visible `DIV` toast rows after the hidden status marker, while preserving Toastify classes, Zoom animation, oldest-first order, WebSocket behavior, and viewport bounds. | Verified |
| User modal markers product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `.user-modal-info-markers` now renders exact old React `user-modal-info-markers mt-2`, removes Vue-only `rankland-user-modal-markers`, and preserves marker label text, preset marker classes, label styling, display, and 8px top margin. | Verified |
| User modal marker label product class parity | `tests/e2e/full-chain/ranklist.spec.ts` verifies that `[data-id="rankland-user-modal-marker"]` now renders exact old React modal class plus SRK preset class `user-modal-info-marker srk-preset-marker-yellow`, removes Vue-only `rankland-user-modal-marker`, and preserves marker text, preset style, inline-block layout, padding, border, radius, font size, and spacing. | Verified |
| User modal rank-time panel product class parity | `tests/e2e/full-chain/live.spec.ts` verifies that `[data-id="rankland-rank-time-panel"]` now renders exact old React `mt-4`, removes Vue-only `rankland-rank-time-panel`, and preserves chart-only chrome, no unit/summary/event nodes, 16px top margin, zero padding/border, G2 metadata, canvas rendering, and curve height. | Verified |
| Rank-time chart wrapper class parity | `tests/e2e/full-chain/live.spec.ts` verifies that `[data-id="rankland-rank-time-curve"]` and `[data-id="rankland-rank-time-g2-chart"]` preserve stable migration hooks while rendering the old plain RankCurve wrapper/G2 container DOM without Vue-only `rankland-rank-time-*` product classes, preserving relative wrapper layout, 400px chart height, G2 metadata, and canvas rendering. | Verified |
| Rank-time chart status attribute parity | `tests/e2e/full-chain/live.spec.ts` verifies that `[data-id="rankland-rank-time-curve"]` no longer renders the Vue-only `data-chart-status` lifecycle attribute while preserving the classless wrapper/G2 container, relative layout, 400px height, G2 metadata, and canvas rendering; `src/client/components/rankland-ranklist.vue` also no longer carries stale `.rankland-rank-time-curve` CSS. | Verified |
| User modal photo inline width parity | `tests/e2e/full-chain/ranklist.spec.ts` now verifies the old React inline `width: 100%` style on `[data-id="rankland-user-modal-photo"]` while preserving computed full-width behavior, `max-width: 100%`, and broken-image hiding coverage. | Verified |
| Ranklist Head metadata full URL parity | `tests/e2e/full-chain/ranklist.spec.ts` covers the loaded `/ranklist/:id` Head contract from old React: title `Test Contest 2024 | RankLand`, `og:title`, absolute `og:url`, and absolute canonical URL using `https://rl.algoux.org/ranklist/test-key`. | Verified |
| Live page parity | `tests/e2e/full-chain/live.spec.ts` covers CSR hydration, old route-local Head with no canonical link while preserving title and loaded-state `og:title`, hidden hydration marker behavior, query preservation, polling, loaded live content old `DIV` wrapper with exact `mt-8 mb-8` class list, no Vue-only `live-content` / `live-content-with-scroll-solution` product classes, 32px spacing, old inline `250px` scroll-solution offset, and mobile realtime content-left `250px`, loaded root `DIV` with no Vue-only `live-page` class and no route-local `70vh` min-height, loaded live wrapper no route padding/no `1280px` max-width/no auto-centering plus old `250px` scroll-solution left offset, WebSocket setup guard, realtime event display, WebSocket error/close reconnect, scroll-solution Switch shape/state, old scroll-toggle wrapper `inline-flex items-center` class tokens, old text span `mr-1`, old 4px text-to-switch spacing from text margin instead of wrapper gap, old 14px and light `rgba(0, 0, 0, 0.85)` text style, old mobile no-toggle-DOM behavior below 768px, old controls root no declared gap and plain block extra-action wrapper chrome, unknown-result `--` fallback with no Vue-only `result-unknown` class, scroll-solution toggle close, query preservation, old Toastify `DIV` container/direct visible `DIV` row DOM, Toastify row/container/Zoom presentation, old Toastify `newestOnTop={false}` oldest-first visible row order, user-modal rank-time chart-only `mt-4` wrapper class chrome plus plain rank-time chart wrapper/G2 container class and status-attribute parity, G2 metadata/canvas, NotFound/error/loading states with old `mt-16 text-center` class tokens and NotFound `h3.mb-4`, normal live screenshots, realtime screenshots, and mobile progress bounds. | Verified |
| Page-level visual review | Full-chain visual/layout tests produce screenshots and assert viewport bounds for app shell, home, search, ranklist, collection, playground, and live normal/realtime views. | Verified |
| Final migration gate | 2026-05-28 SRK controls empty extra-action DOM parity has focused RED/GREEN evidence for restoring the old React always-present empty right-side controls `DIV` on normal Ranklist routes while preserving filter behavior; full gate used Node `v24.11.1` and pnpm `8.15.9`, `gen:client-router` generated 6 client routes, `corepack pnpm test:migration` passed with build, 36 unit files / 154 unit tests, 1 SSR smoke test, 1 shallow Playwright test, and 60 passed / 1 skipped full-chain Playwright tests; `git diff --check` passed. | Verified |
| Production deployment, remote push, branch merge, or old implementation deletion | Explicitly out of scope for this migration working branch unless requested by Cooper. | Not performed |

## Deferred Product Decisions

These are not hidden blockers for the currently verified product-restoration surface. They are explicit follow-up decisions or review-driven areas.

- Home: broader SEO/content polish beyond verified structured data, SSR content, legacy `main.normal-content` / `.home-intro` wrapper DOM, legacy `div.block` content group DOM, legacy normal-content/block spacing, legacy hero title inline `font-size: 32px`, legacy hero paragraph `text-base` class token, legacy recommendation/tool card row horizontal-only gutter with `rowGap: normal`, legacy recommendation/tool card column `mb-4` class tokens plus 16px bottom spacing, legacy tool logo `mr-3 inline-block` class/display parity, legacy about separator `mx-2` class/spacing parity, legacy `h1.block-title` section headings, legacy statistics nullish fallback, legacy statistics `strong` presentation, Ant Design content layout, recommendation card dark Ant Design Card token parity, non-card dark text color parity, recommendation card heading/icon parity, card title icon/logo spacing parity, card paragraph `mt-4 mb-0` spacing parity, paste.then.ac logo padding parity, default visible external link `target="_blank"` plus omitted `rel` parity, conditional beian link `target="_blank"` plus omitted `rel` parity, shared Ant Design Vue contact modal behavior, old ContactUs no-`href` anchor trigger DOM, and shared contact QQ image `w-full` class remains accepted as non-blocking.
- Search: old route-local Head omits canonical link while preserving title and `og:title`, old root `div.normal-content`, plain inner `div`, no Vue-only `.search-page`, no `section.search-panel`, no route-local `min-height: 70vh`, old exact `h3.mb-6` heading class, no Vue-only `.search-heading`, old search input no custom page class and no Vue-only `.search-input`, old result/recent list items no custom page class and no Vue-only `.search-list-item`, old loading/error/result/recent state wrappers using only `mt-10`, result/recent `DIV` wrappers, no Vue-only `.search-state` / `.search-section`, result/recent title nodes as old `div.opacity-70`, result summary exact old plain text `搜索到 N 个结果`, no Vue-only `[data-id="search-result-count"]` span, preserved result section `data-result-count`, old keyword whitespace passthrough semantics for route `kw`, input value, submitted search URL, and Fuse search, whitespace-only `kw` remains a result state, list spacing as old `div.mt-2 > .ant-list`, recent empty state as old `div.mt-2`, no Vue-only `.search-section-title` / `.search-list` / `.search-empty-state`, result/recent row content exact old `p.mb-0`, `span.ml-2.opacity-70`, and `p.mb-0.opacity-50.text-sm` classes, no Vue-only `.search-row-title` / `.search-view-count` / `.search-created-at`, load-error inner message exact old `div.text-red-500` class, no Vue-only `.search-error-message`, old boolean `enterButton` default SearchOutlined icon button, old loading/error state utility class tokens, old load-error outer `mt-10` wrapper plus inner `text-red-500` message DOM/color, and old result/recent list utility class tokens are now verified alongside prior normal-content, Ant Design Input.Search/List/Spin, hydration marker, recent/result rows, and zero-result behavior.
- Ranklist: loaded-route Head now matches old React with `og:title`, absolute `og:url`, and absolute canonical URL; loaded route root and content wrapper now match old React as plain `DIV` nodes, the content class list is exactly `mt-8 mb-8`, and the Vue-only `ranklist-content` product class is removed while preserving 32px top/bottom spacing. Test-only hydration markers are still visually hidden while preserving full-chain selectors and `hydrated` text assertions. Collection hydration markers are likewise visually hidden.
- Collection: loaded-state Head now matches old React with `og:title`, absolute `og:url`, and absolute canonical URL; loaded root now matches old React as a plain `DIV`; loaded shell, nav, hidden header, ranklist panel, and selected-ranklist content now match old React `DIV` wrappers with exact `srk-collection-container`, `srk-collection-nav`, `srk-collection-hidden-header`, `srk-collection-ranklist`, and `pb-8` class lists; Vue-only `collection-page`, `collection-nav`, `collection-hidden-header`, `collection-ranklist-panel`, and `collection-ranklist-content` product classes are removed. The loaded shell root now also removes Vue-only root chrome and computes `position: static` / `min-height: 0px`. Old selected-ranklist 32px bottom padding, old `.srk-collection-ranklist` `position: relative` / `flex: 1`, old collapsed submenu-title 0px horizontal padding, nav `z-index: 1`, hidden-header `z-index: auto`, old light `#f4f4f4` and dark `#111111` nav background, dark `#434343` nav border, old `.srk-collection-hidden-header h3.mb-0` hidden-title DOM, old hidden-title no-inline-`marginTop`, old expanded `marginLeft: 8px`, collapsed `fontSize: 14px` / `marginLeft: 0px`, and no Vue-only `line-height: 1` are now verified alongside prior Ant Design Vue menu/category icon/mobile collapse/remaining-height behavior.
- Live: old route-local Head omits canonical link while preserving title and loaded-state `og:title`; loaded live route content now matches old React as a plain `DIV` with exact `mt-8 mb-8` class list, no Vue-only `live-content` / `live-content-with-scroll-solution` product classes, 32px top/bottom spacing, old inline `250px` scroll-solution offset, and mobile realtime content-left `250px`; loaded root now matches old React as a plain `DIV` with no Vue-only `live-page` class and no route-local `70vh` min-height; loaded live route wrapper chrome now has no Vue-only page padding, no `1280px` max-width cap, no auto-centering, and preserves the old `250px` scroll-solution left offset with no extra right margin. Old scroll-solution toggle wrapper `inline-flex items-center` classes, text span `mr-1`, 4px text-to-switch margin, and normal wrapper gap are verified, the toggle text style is verified at 14px plus light `rgba(0, 0, 0, 0.85)`, mobile below 768px now follows old React by not rendering the scroll-solution toggle DOM, unknown realtime results keep the old `--` text with no extra `result-unknown` class, and visible realtime rows now keep old Toastify `DIV` row DOM plus `newestOnTop={false}` oldest-first order, alongside prior Ant Design Vue Switch shape/state, WebSocket reconnect/close, Toastify container, and realtime layout behavior.
- Playground: exact old Monaco `0.34.0` package-version parity is intentionally not preserved because the verified Vue wrapper requires Monaco `>=0.43.0`; synthetic Monaco editing remains a harness limitation, while the product editor path, old route-local Head without canonical output, old Monaco minimap/default option chrome, old route wrapper `DIV` with no Vue-only `.playground-page` class or route-local `70vh` min-height, old route-level no-class client wrapper after the hidden hydration marker, old exact `DIV.srk-playground-container` as that wrapper's direct child with no Vue-only `.playground-layout`, old `.srk-playground-preview` shell class, old desktop flex/500px layout, no Vue-only editor wrapper border/radius, welcome modal old right-top `QuestionCircleOutlined` docs cue copy, invalid prompt `h3.mt-16.text-center` plus shortcut tag `mr-0` class token and 0px right/left spacing, old valid SRK preview wrapper `div.mt-8.mb-8`, old checker-error exact `div.ml-8 > h3 + pre` DOM/class with no Vue-only `.rankland-ranklist-check-error` product class, absence of the Vue-only toolbar/heading and visible Preview button, hidden hydration/editor-ready markers, preview-pane `div.absolute.right-4.top-4` wrapper with exact class `absolute right-4 top-4` around the plain no-class `QuestionCircleOutlined` docs anchor with `target="_blank"` and omitted `rel`, and stable preview coverage are verified by build/hydration and full-chain tests. Mobile keeps the migrated no-horizontal-overflow guard.
- SRK renderer wrapper: Vue-only `.rankland-ranklist` and `.rankland-ranklist-header` wrappers are removed, so the normal route now exposes the old React direct top-level child order; old header banner wrapper `flex items-center justify-center` renders without Vue-only `.rankland-ranklist-banner-wrap`, preserves `display:flex`, `align-items:center`, `justify-content:center`, and keeps 0px bottom margin while banner image restores exact old `mb-2` class, inline `max-width: min(100%, 1820px)` / `max-height: 40vh`, no Vue-only `rankland-ranklist-banner` product class, and old 8px bottom margin; title `text-center mb-1`, meta row exact `text-center mt-1` now renders without Vue-only `.rankland-ranklist-header-meta`, contributors `mb-0`, ref-links, and time `text-center mb-0`; contributors and ref-links now render as children of the old React metadata block instead of Vue-only sibling nodes; old render-error fallback now uses the old plain `div role="alert"` wrapper with inline `max-width: 400px; margin: 100px auto`, no Vue-only `rankland-ranklist-error` product class, and Ant Design Alert message/description/icon behavior; old progress wrapper `mx-4` spacing, old ranklist/live loaded route content `mt-8 mb-8` class tokens and 32px top/bottom spacing, old broken SRK banner/user-photo image hiding behavior, old export action `border-0 border-solid border-gray-400 mr-2` plus conditional `pl-2 border-l`, old share action `pl-2 border-0 border-l border-solid border-gray-400`, old header action no-gap spacing and block/inline display, old header title typography is now verified at 32px/500/4px, old header meta/contributors/ref-links/time text size is verified at 14px, old contributor visible item `span` DOM is verified, old contributor link `target="_blank"` / `rel="noopener"` DOM is verified, old ref-links `span` wrapper DOM and visible ref-link item `span` DOM are verified, old visible and hidden ref-link `target="_blank"` / `rel="noopener"` DOM is verified, old footer GitHub/Standard Ranklist/榜单合集 external package/project links are verified with `target="_blank"` and omitted `rel`, old footer beian link is verified with `target="_blank"` and omitted `rel`, old footer ContactUs no-`href` anchor trigger DOM is verified, old Eye icon view-count, exact old `mr-2` class list with no Vue-only `.rankland-ranklist-view-count`, missing-`viewCnt` `-` fallback, and hidden ref-link trigger zero CSS left margin are verified, old header export/share action icons are verified with light/dark primary plus hover colors, old controls root `mt-3 mx-4 flex justify-between items-center`, Select `ml-2`, official-filter `ml-5 inline-flex items-center`, official label `mr-1`, marker filter `ml-5 inline-flex items-center`, old table spacer `div.mt-6`, old remarks wrapper exact `mb-4 text-center` with no Vue-only `rankland-ranklist-remarks`, old footer root `text-center mt-8` and paragraph `mb-0` / `mt-1 mb-0` classes are verified, user and solution modal wrappers now live inside the table wrapper and their roots preserve old `srk-react-modal-root` alongside the animated/general modal root classes, and user-modal body restores the old `.user-modal` root class while the organization line restores old `mb-0` and users without organization keep the old empty `p.mb-0` organization line, the team-members row restores old `mt-2` with raw ` / ` separator text and old item-level entry span DOM, the markers row restores old `mt-2`, the unofficial line and award line both restore old `mt-4 mb-0`, the award label restores the old `.user-modal-segment-label` class, the slogan restores the old `.slogan mt-4 mb-2` classes, and the photo wrapper restores the old `mt-4` class plus old shared photo/slogan parent DOM while preserving full-width image behavior, including the old empty photo/slogan wrapper for users without photo/slogan; rank-time is restored to the old chart-only `mt-4` wrapper class and G2 tooltip/animation behavior; remaining lower-level exact table pixel parity should be handled only when product review identifies a concrete difference.
- Production deployment, remote push, branch merge, and old implementation deletion remain out of scope unless requested.

## Final Gate Commands

Run these from `/Users/cooper/Projects/RankLand/rankland-web` with Node 24 and pnpm 8:

```bash
node -v
corepack pnpm -v
corepack pnpm run gen:client-router
corepack pnpm test:migration
git diff --check
```

The final review is accepted only after the fresh commands pass and generated router outputs do not drift unexpectedly.

## Final Gate Result

Fresh verification on 2026-05-28:

- `node -v`: `v24.11.1`
- `corepack pnpm -v`: `8.15.9`
- `corepack pnpm run gen:client-router`: generated 6 client routes, exited cleanly; warning only: `Skipped due to language "js" is not supported. Only [ts,tsx] are supported (file: src/client/modules/fallback/not-found.view.vue)`
- `corepack pnpm test:migration`: passed with build, 36 unit files / 154 unit tests, 1 SSR smoke test, 1 shallow Playwright test, and 60 passed / 1 skipped full-chain Playwright tests
- `git diff --check`: passed

Latest slice: SRK controls empty extra-action DOM parity. Focused RED reproduced normal Ranklist controls rendering only the filters child and omitting the old React always-present empty right-side `DIV`; focused GREEN verified `[data-id="rankland-ranklist-extra-action"]` renders as an empty plain `DIV` under controls while preserving Ant Design filter controls and filtering behavior. Full gate passed with Node `v24.11.1`, pnpm `8.15.9`, generated client routes, `test:migration`, and `git diff --check`.
